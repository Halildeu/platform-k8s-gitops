# schema-service config — overlays'de namespace-spesifik değerler override edilir
apiVersion: v1
kind: ConfigMap
metadata:
  name: schema-service-config
data:
  # ADR-0005 Dual DataSource Reporting (Faz 19.MSSQL activation)
  # Primary spring.datasource = Workcube MSSQL (schema introspection lane)
  #   → SPRING_DATASOURCE_URL/USERNAME/PASSWORD ESO secret schema-service-mssql-secrets'ten gelir
  # Secondary report.postgres = platform metadata (eğer kullanılıyorsa)
  REPORT_PG_URL: "jdbc:postgresql://postgres:5432/schemas_db"
  SCHEMA_MSSQL_ENABLED: "true"   # feature flag

  # gitops#3594 — IFS ERP Oracle catalog source. Off in base: it needs both the
  # Vault path (kv/platform/oracle-ifs) and the egress NetworkPolicy, so only an
  # overlay that has provisioned both turns it on. JDBC URL, username and
  # password arrive from the ExternalSecret, never from here.
  SCHEMA_ORACLE_ENABLED: "false"
  SCHEMA_ORACLE_SOURCE_ID: "ifs"
  # IFS publishes business data through the IFSAPP view layer.
  SCHEMA_ORACLE_DEFAULT_SCHEMA: "IFSAPP"

  # Spring Boot
  SERVER_PORT: "8096"
  # Actuator ayrı port (Spring Boot native): 8081'de yayınlanır, Spring Security
  # filter chain main port'a attached — management port serbest (standart pattern)
  MANAGEMENT_SERVER_PORT: "8081"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "health,info,prometheus"
  MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED: "true"
  MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: "never"

  # Graceful shutdown — Codex Tur-3 #6
  SERVER_SHUTDOWN: "graceful"
  SPRING_LIFECYCLE_TIMEOUT_PER_SHUTDOWN_PHASE: "30s"

  # PG connection pool — canlı gözlem (follow-up #7)
  SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: "5"
  SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE: "2"

  # D7 Eureka kapatma (ana repo Eureka temizliği gelene kadar runtime override)
  # Bu env'ler main-stable image'ındaki Eureka client'ı devre dışı bırakır.
  EUREKA_CLIENT_ENABLED: "false"
  SPRING_CLOUD_DISCOVERY_ENABLED: "false"
  SPRING_CLOUD_SERVICE_REGISTRY_AUTO_REGISTRATION_ENABLED: "false"

  # Tracing (Tempo için; base'de kapalı, monitoring kurulunca aç)
  MANAGEMENT_TRACING_ENABLED: "false"
  MANAGEMENT_TRACING_SAMPLING_PROBABILITY: "0.1"

  # Logging level (prod'da INFO, test'te DEBUG override edilir)
  LOGGING_LEVEL_ROOT: "INFO"
  LOGGING_LEVEL_COM_ACIK: "INFO"
  # 2026-05-04 Session 37: KC OIDC config — was missing in base, caused
  # JWT validation to fail on prod (anonymous SecurityContext, 401 on
  # all authenticated endpoints). Mirrors api-gateway/permission-service
  # pattern. Test cluster uses internal KC svc DNS; prod overlay overrides
  # ISSUER to ai.acik.com (see overlays/prod/kustomization.yaml patches).
  KEYCLOAK_ISSUER_URI: "http://keycloak:8080/realms/serban"
  KEYCLOAK_JWKS_URI: "http://keycloak:8080/realms/serban/protocol/openid-connect/certs"

  # 2026-09-10 gitops#3608: REPORT module gate on /api/v1/schema/** — the
  # API-side twin of the shell's REPORT route guard. schema-service asks
  # permission-service /authz/me with the caller's own bearer and allows only
  # when that projection grants REPORT (platform-backend#1145). Base URL is the
  # intra-ns shortname report-service already uses. The image's k8s profile
  # defaults the gate to on and refuses to start with it off; ENABLED is pinned
  # here so the intent is visible in gitops, not because the image needs it.
  PERMISSION_SERVICE_BASE_URL: "http://permission-service:8090"
  SCHEMA_REPORT_GATE_ENABLED: "true"
