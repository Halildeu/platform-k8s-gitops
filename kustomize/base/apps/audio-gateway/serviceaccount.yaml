# audio-gateway-service ServiceAccount — Faz 24
# imagePullSecret bağlanır (D26 pattern, ghcr-pull)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: audio-gateway
  labels:
    app.kubernetes.io/name: audio-gateway
imagePullSecrets:
  - name: ghcr-pull
automountServiceAccountToken: false
