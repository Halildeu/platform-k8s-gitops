apiVersion: v1
kind: Service
metadata:
  name: etik-speak-manager
  labels:
    app.kubernetes.io/name: etik-speak-manager
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: etik-speak-manager}
  ports:
    - {name: http, port: 80, targetPort: http, protocol: TCP}
