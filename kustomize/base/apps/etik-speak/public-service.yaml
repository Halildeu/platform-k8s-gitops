apiVersion: v1
kind: Service
metadata:
  name: etik-speak-public
  labels:
    app.kubernetes.io/name: etik-speak-public
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: etik-speak-public}
  ports:
    - {name: http, port: 80, targetPort: http, protocol: TCP}
