# Faz 19.9 cutover: platform-web-frontend[-testai] private GHCR image
# ghcr-pull imagePullSecret gerekli (ESO → Vault kv/gitops/ghcr-token).
# Eski: nginx:1.27-alpine public → yeni: platform-web-frontend private.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend
automountServiceAccountToken: false
imagePullSecrets:
  - name: ghcr-pull
