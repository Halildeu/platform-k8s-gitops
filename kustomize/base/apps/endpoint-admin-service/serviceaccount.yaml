# endpoint-admin-service ServiceAccount — Faz 22 (sprint PR-9)
# imagePullSecret bağlanır (D26 pattern, ghcr-pull)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: endpoint-admin-service
  labels:
    app.kubernetes.io/name: endpoint-admin-service
imagePullSecrets:
  - name: ghcr-pull
automountServiceAccountToken: false
