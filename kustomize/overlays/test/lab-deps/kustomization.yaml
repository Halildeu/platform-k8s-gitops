# Test cluster lab dependencies for D29-NOTIFY-Functional evidence
# Faz 23.1 M2 closure (2026-05-12 target)
# Codex 019e0c28 hibrit B + A-minimal absorb
#
# Bu overlay sadece test cluster için; prod'a deploy edilmez.
# notification-orchestrator test overlay env config zaten Mailpit'e pointliyor:
#   NOTIFY_ADAPTERS_SMTP_HOST=mailpit.platform-test.svc.cluster.local
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mailpit.yaml
  - webhook-receiver.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/managed-by: notify-d29-lab-deps
