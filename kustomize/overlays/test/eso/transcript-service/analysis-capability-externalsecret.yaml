# transcript-service analysis capability issuer key - TEST only (gitops#3144).
#
# Keep this trust root isolated from the service's DB/Redis/auth
# ExternalSecret. A missing or delayed capability property must block analysis
# issuance without blocking unrelated credential refresh.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: transcript-service-analysis-capability
  namespace: platform-test
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: transcript-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: platform
    platform.acik.com/faz: "24"
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-platform-gitops
  target:
    name: transcript-service-analysis-capability
    creationPolicy: Owner
  data:
    - secretKey: ANALYSIS_JOB_CAPABILITY_HMAC_SECRET
      remoteRef:
        key: kv/platform/meeting-analysis-capability
        property: hmac_secret_base64
