# endpoint-admin-remote-bridge-device-key LIVE wrapper — Faz 22.6 #548 A1
#
# This wrapper intentionally has no nameSuffix. The child overlay creates the
# dedicated device-key broker resources with the -device-key suffix; this wrapper
# also owns the existing public SNI ingress name and points it at that dedicated
# broker service. Apply this wrapper for the live #548 device-key run.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-test

resources:
  - ../endpoint-admin-remote-bridge-device-key
  - ingress-passthrough.yaml
