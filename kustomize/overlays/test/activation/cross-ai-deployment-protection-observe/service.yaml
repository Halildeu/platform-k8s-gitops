apiVersion: v1
kind: Service
metadata:
  name: cross-ai-deployment-protection
  labels:
    app.kubernetes.io/name: cross-ai-deployment-protection
    app.kubernetes.io/component: github-app-observer
    app.kubernetes.io/part-of: platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cross-ai-deployment-protection
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
