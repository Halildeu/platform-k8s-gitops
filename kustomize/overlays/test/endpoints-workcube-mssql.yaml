# Faz 20 LIVE — test cluster direct routing (bridge proxy decommissioned)
# Calico containerIPForwarding=Enabled (PR #136) → pod direct → 10.9.193.201:1433
# Önceki: 172.19.0.8:11433 (workcube-mssql-proxy-test alpine/socat bridge container)
apiVersion: v1
kind: Endpoints
metadata:
  name: workcube-mssql
  labels:
    app.kubernetes.io/name: workcube-mssql
subsets:
  - addresses:
      - ip: 10.9.193.201              # Workcube MSSQL host direct (Faz 20 LIVE)
    ports:
      - name: mssql
        port: 1433                    # MSSQL standart port (TDS)
        protocol: TCP
