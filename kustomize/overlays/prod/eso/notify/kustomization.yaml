# notification-orchestrator ESO prod overlay — Faz 23.9 Step D
# (Codex thread `019e0892`). Activates flat-path Vault integration for prod
# notification-orchestrator-secrets. Replaces direct-Secret bootstrap that was
# created during the 2026-05-08 cutover when Vault setup was deferred.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-prod

resources:
  - externalsecret-notify.yaml
