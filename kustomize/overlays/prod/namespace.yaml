# Namespace — platform-prod (k3d-prod, staging-sw-2 D32)
# Labels: ApplicationSet cluster generator match prereq
apiVersion: v1
kind: Namespace
metadata:
  name: platform-prod
  labels:
    app.kubernetes.io/part-of: platform
    app.kubernetes.io/cluster: prod
    platform: "true"
    env: prod
