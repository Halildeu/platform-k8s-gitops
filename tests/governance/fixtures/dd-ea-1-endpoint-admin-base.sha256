# DD-EA-1 baseline (kustomize render bytes sha256)
# Source: kustomize/base/apps/endpoint-admin-service
# Update: python3 scripts/governance/check_endpoint_admin_manifest_contract.py --update-baseline
#
# 2026-06-04 OpenFGA provenance fix (gitops #1265 / PR #1266): base ConfigMap now
#   tracks ERP_OPENFGA_ENABLED + ERP_OPENFGA_API_URL + empty ERP_OPENFGA_STORE_ID
#   / ERP_OPENFGA_MODEL_ID stubs (were live-only ConfigMap drift; the @RequireModule
#   gate was ON at runtime but invisible in gitops -> silent-flip risk). Codex
#   019e92cb AGREE. Previous baseline:
#   81978377ba94e0ce65b3f43133e099de93c5f012f1c69f4e44aa8345329c529d
#
# 2026-06-05 AG-028 go-live (gitops #1239 / PR #1279): base ConfigMap adds the
#   explicit ENDPOINT_ADMIN_UNINSTALL_ENABLED="false" dark-launch default (the
#   test overlay JSON6902-replaces it to "true"). Codex 019e946d AGREE. Previous
#   baseline:
#   1d73da23ee251a2864b4a83b895c2877877565b183b572e0b2670937571c1da5
#
# 2026-06-07 OpenFGA ESO migration (gitops #1267): base ConfigMap keeps
#   ERP_OPENFGA_ENABLED/API_URL but no longer carries STORE_ID/MODEL_ID stubs.
#   endpoint-admin store/model now come from the ESO-managed
#   endpoint-admin-service-secrets Secret via kv/platform/openfga, matching the
#   shared runtime-artifact selector. Previous baseline:
#   9477c3e41fd04de81d16e589c94ce7d0b9e24a05ad65dc8748d9b623a8182f32
#
# 2026-06-07 OpenFGA runtime reconcile (gitops #1267/#1331): endpoint-admin
#   ConfigMap now carries ArgoCD Replace=true so stale SSA-managed
#   ERP_OPENFGA_STORE_ID/MODEL_ID data keys are removed from live ConfigMap, and
#   Deployment pod template carries the stable ESO selector marker to roll pods
#   and re-read envFrom after the selector migration. Previous baseline:
#   b95f55ca030c67d0105ddcc9b938bbc78dcc380a0e5580d13309f51554795600
#
# 2026-06-14 endpoint mTLS host prep (gitops #1359 / PR #1540): base
#   Deployment declares a default-off mtls connector port, optional mTLS
#   credential envFrom Secret, and optional read-only PKCS12 store mount; base
#   ConfigMap carries default-off passthrough settings. Runtime activation still
#   requires test overlay/edge/operator gates; Claude CLI cross-AI review
#   verdict AGREE. Previous baseline:
#   0f52701ae952f1dd5805f3e90dca7bf5a4cd8ad50ce7142026f49be3b026241d
b8740ea7a27798879fbb82ac8077e59f90e58737b0094aa4b62e3652952b186a
