name: Faz 24 Zeynep Meeting Intelligence Access

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "dry-run validates; repair-persona repairs/rotates the test writer; apply grants bounded access"
        required: true
        type: choice
        options:
          - dry-run
          - repair-persona
          - apply
        default: dry-run

permissions:
  contents: read

concurrency:
  group: faz24-zeynep-meeting-access
  cancel-in-progress: false

jobs:
  provision:
    name: exact identity + least-privilege access
    runs-on: [self-hosted, staging-sw, testai-deploy]
    timeout-minutes: 10
    env:
      OUT_PATH: /tmp/faz24-zeynep-meeting-access-${{ github.run_id }}.json
      KC_ADMIN_PASSWORD: ${{ secrets.KC_TEST_ADMIN_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Provision and verify
        env:
          MODE: ${{ inputs.mode }}
        run: |
          set -euo pipefail
          if [ "${MODE}" = "repair-persona" ]; then
            bash scripts/faz24/repair-d35-permission-writer-credential.sh --out "${OUT_PATH}"
          else
            args=(--out "${OUT_PATH}")
            if [ "${MODE}" = "apply" ]; then
              args+=(--apply)
            fi
            bash scripts/faz24/provision-meeting-intelligence-access.sh "${args[@]}"
          fi

      - name: Verify redacted result contract
        id: redaction
        if: always()
        run: |
          set -euo pipefail
          test -s "${OUT_PATH}"
          jq -e '
            (.schemaVersion == "faz24.meetingIntelligenceAccess.v1" or
             .schemaVersion == "faz24.permissionWriterCredentialRepair.v1" or
             .schemaVersion == "faz24.permissionWriterCredentialRepair.v2" or
             .schemaVersion == "faz24.permissionWriterCredentialRepair.v3") and
            .boundaries.productionMutation == false and
            .boundaries.rawIdentityIncluded == false and
            .boundaries.rawCredentialIncluded == false and
            .boundaries.rawTokenIncluded == false and
            (if .schemaVersion == "faz24.meetingIntelligenceAccess.v1" then
              .boundaries.broadAdminGrant == false and
              (.target | has("email") | not) and
              (.target | has("userId") | not)
             else
              .boundaries.targetUserMutation == false and
              (if .schemaVersion == "faz24.permissionWriterCredentialRepair.v3" then
                (.boundaries.permissionWriterEmailMutation | type == "boolean") and
                .permissionWriter.identityBinding == "username+immutable-user-id" and
                (.permissionWriter.requiredActionsReady | type == "boolean") and
                (.permissionWriter.profileReady | type == "boolean") and
                (.permissionWriter.profileRepaired | type == "boolean") and
                (.permissionWriter.profileEmailCollisionFree | type == "boolean") and
                (.permissionWriter.existingAttributesPreserved | type == "boolean") and
                (.permissionWriter.requiredActionsPreserved | type == "boolean") and
                ((.permissionWriter.profileReady == false) or
                 (.permissionWriter.requiredActionsReady == true and
                  .permissionWriter.profileEmailCollisionFree == true and
                  .permissionWriter.existingAttributesPreserved == true and
                  .permissionWriter.requiredActionsPreserved == true))
               elif .schemaVersion == "faz24.permissionWriterCredentialRepair.v2" then
                .boundaries.permissionWriterEmailMutation == false and
                .permissionWriter.identityBinding == "username+immutable-user-id" and
                (.permissionWriter.requiredActionsReady | type == "boolean") and
                (.permissionWriter.requiredActionsCleared | type == "boolean")
               else
                true
               end)
             end)
          ' "${OUT_PATH}" >/dev/null

          if grep -aEq \
            -e 'Bearer ' \
            -e 'Authorization:' \
            -e 'eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.' \
            -e '"(password|secret|access_token|refresh_token|email|userId)"[[:space:]]*:' \
            "${OUT_PATH}"; then
            echo "::error::result contains forbidden identity or credential material"
            exit 1
          fi

      - name: Publish redacted summary
        if: ${{ always() && steps.redaction.outcome == 'success' }}
        run: |
          set -euo pipefail
          if [ ! -s "${OUT_PATH}" ]; then
            echo "## Faz 24 Meeting Intelligence access" >> "${GITHUB_STEP_SUMMARY}"
            echo "- Result: missing" >> "${GITHUB_STEP_SUMMARY}"
            exit 0
          fi
          {
            echo "## Faz 24 Meeting Intelligence access"
            echo
            echo "- Mode: \`$(jq -r .mode "${OUT_PATH}")\`"
            echo "- Status: \`$(jq -r .status "${OUT_PATH}")\`"
            echo "- Failure: \`$(jq -r '.failureReason // "none"' "${OUT_PATH}")\`"
            if [ "$(jq -r .schemaVersion "${OUT_PATH}")" = "faz24.permissionWriterCredentialRepair.v3" ]; then
              echo "- Exact writer identity: \`$(jq -r .permissionWriter.exactIdentityMatch "${OUT_PATH}")\`"
              echo "- Writer identity binding: \`$(jq -r .permissionWriter.identityBinding "${OUT_PATH}")\`"
              echo "- Writer email mutation: \`$(jq -r .boundaries.permissionWriterEmailMutation "${OUT_PATH}")\`"
              echo "- Writer required actions ready: \`$(jq -r .permissionWriter.requiredActionsReady "${OUT_PATH}")\`"
              echo "- Writer profile ready: \`$(jq -r .permissionWriter.profileReady "${OUT_PATH}")\`"
              echo "- Writer profile repaired: \`$(jq -r .permissionWriter.profileRepaired "${OUT_PATH}")\`"
              echo "- Writer email collision-free: \`$(jq -r .permissionWriter.profileEmailCollisionFree "${OUT_PATH}")\`"
              echo "- Writer attributes preserved: \`$(jq -r .permissionWriter.existingAttributesPreserved "${OUT_PATH}")\`"
              echo "- Writer required actions preserved: \`$(jq -r .permissionWriter.requiredActionsPreserved "${OUT_PATH}")\`"
              echo "- Keycloak credential reset: \`$(jq -r .permissionWriter.keycloakCredentialReset "${OUT_PATH}")\`"
              echo "- Vault record synced: \`$(jq -r .permissionWriter.vaultRecordSynced "${OUT_PATH}")\`"
              echo "- Vault rollback attempted: \`$(jq -r .permissionWriter.vaultRollbackAttempted "${OUT_PATH}")\`"
              echo "- Vault rollback succeeded: \`$(jq -r .permissionWriter.vaultRollbackSucceeded "${OUT_PATH}")\`"
              echo "- Writer login readback: \`$(jq -r .permissionWriter.loginReady "${OUT_PATH}")\`"
              echo "- Permission-service roles readback: \`$(jq -r .permissionWriter.rolesReadReady "${OUT_PATH}")\`"
            elif [ "$(jq -r .schemaVersion "${OUT_PATH}")" = "faz24.permissionWriterCredentialRepair.v2" ]; then
              echo "- Exact writer identity: \`$(jq -r .permissionWriter.exactIdentityMatch "${OUT_PATH}")\`"
              echo "- Writer identity binding: \`$(jq -r .permissionWriter.identityBinding "${OUT_PATH}")\`"
              echo "- Writer email mutation: \`$(jq -r .boundaries.permissionWriterEmailMutation "${OUT_PATH}")\`"
              echo "- Writer required actions ready: \`$(jq -r .permissionWriter.requiredActionsReady "${OUT_PATH}")\`"
              echo "- Writer required actions cleared: \`$(jq -r .permissionWriter.requiredActionsCleared "${OUT_PATH}")\`"
              echo "- Keycloak credential reset: \`$(jq -r .permissionWriter.keycloakCredentialReset "${OUT_PATH}")\`"
              echo "- Vault record synced: \`$(jq -r .permissionWriter.vaultRecordSynced "${OUT_PATH}")\`"
              echo "- Vault rollback attempted: \`$(jq -r .permissionWriter.vaultRollbackAttempted "${OUT_PATH}")\`"
              echo "- Vault rollback succeeded: \`$(jq -r .permissionWriter.vaultRollbackSucceeded "${OUT_PATH}")\`"
              echo "- Writer login readback: \`$(jq -r .permissionWriter.loginReady "${OUT_PATH}")\`"
              echo "- Permission-service roles readback: \`$(jq -r .permissionWriter.rolesReadReady "${OUT_PATH}")\`"
            elif [ "$(jq -r .schemaVersion "${OUT_PATH}")" = "faz24.permissionWriterCredentialRepair.v1" ]; then
              echo "- Exact writer identity: \`$(jq -r .permissionWriter.exactIdentityMatch "${OUT_PATH}")\`"
              echo "- Canonical writer email ready: \`$(jq -r .permissionWriter.canonicalEmailReady "${OUT_PATH}")\`"
              echo "- Writer email reconciliation attempted: \`$(jq -r .permissionWriter.emailReconciliationAttempted "${OUT_PATH}")\`"
              echo "- Writer email reconciliation succeeded: \`$(jq -r .permissionWriter.emailReconciliationSucceeded "${OUT_PATH}")\`"
              echo "- Keycloak credential reset: \`$(jq -r .permissionWriter.keycloakCredentialReset "${OUT_PATH}")\`"
              echo "- Vault record synced: \`$(jq -r .permissionWriter.vaultRecordSynced "${OUT_PATH}")\`"
              echo "- Vault rollback attempted: \`$(jq -r .permissionWriter.vaultRollbackAttempted "${OUT_PATH}")\`"
              echo "- Vault rollback succeeded: \`$(jq -r .permissionWriter.vaultRollbackSucceeded "${OUT_PATH}")\`"
              echo "- Writer login readback: \`$(jq -r .permissionWriter.loginReady "${OUT_PATH}")\`"
              echo "- Permission-service roles readback: \`$(jq -r .permissionWriter.rolesReadReady "${OUT_PATH}")\`"
            else
              echo "- Exact Keycloak identity: \`$(jq -r .target.exactKeycloakMatch "${OUT_PATH}")\`"
              echo "- Exact user-service identity: \`$(jq -r .target.exactUserServiceMatch "${OUT_PATH}")\`"
              echo "- Realm role readback: \`$(jq -r .keycloak.realmRoleReady "${OUT_PATH}")\`"
              echo "- Client role readback: \`$(jq -r .keycloak.clientRoleReady "${OUT_PATH}")\`"
              echo "- Granule readback: \`$(jq -r .permissionService.granulesReady "${OUT_PATH}")\`"
              echo "- Membership readback: \`$(jq -r .permissionService.membershipReady "${OUT_PATH}")\`"
            fi
            echo
            echo "Boundary: platform-test only; no production mutation and no raw identity/credential/token evidence."
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Upload redacted evidence
        if: ${{ always() && steps.redaction.outcome == 'success' }}
        uses: actions/upload-artifact@v7
        with:
          name: faz24-zeynep-meeting-access-${{ github.run_id }}
          path: ${{ env.OUT_PATH }}
          if-no-files-found: error
          retention-days: 14

      - name: Cleanup runner file
        if: always()
        run: rm -f "${OUT_PATH}"
