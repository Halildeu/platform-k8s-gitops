# Faz 19.3 — Zanzibar Batch CI Scope + OpenFGA DSL Validate

**Date:** 2026-04-24
**Scope:** platform-backend Batch 2 — Zanzibar plane migration CI scope + DSL validation.
**Codex AGREE:** thread `019dc0d8` (19.3 plan detail).

---

## 1. Codex 19.3 verdict

> "19.3'te 'kod zaten taşındı' doğru; özel iş yükü artık **(a)** Zanzibar plane modüllerini yeni repo içinde **derlenebilir + koordinat-stabil** hale getirmek, **(b)** `openfga-runtime` DSL'i CI'da **validate** etmek, **(c)** D29 için **testai üstünden** sentetik allow/deny kanıt yolunu netleştirmek."

## 2. Scope delta

Faz 19.1 filter-repo migration ile platform-backend'e taşınan Zanzibar kaynak kod:
- `common-auth/` (Java module, com.example:common-auth:1.1.0)
- `permission-service/` (Spring Boot service)
- `openfga/` (non-Maven dir: model.fga DSL + init.sh + migrate-permissions.py + render_model_json.py + sync-from-keycloak.sh + tuples-seed.json)

Faz 19.3 fark = kod yok, CI scope + DSL validate + runtime kontratını koruma.

## 3. platform-backend PR #2

URL: https://github.com/Halildeu/platform-backend/pull/2

### Changes

**`.github/workflows/ci-mvn-check.yml` update:**
- Batch scope genişletildi: auth-service + user-service + variant-service → **+ permission-service + common-auth** (-am deps otomatik)
- Timeout 15 → 20 dakika (Zanzibar plane genişleme)

**Yeni job `openfga-dsl-validate`:**
- `fga` CLI v0.6.0 install
- `fga model validate --file openfga/model.fga` → syntax + semantic parse
- `fga model transform --output-format json` → model.json artefact
- Artifact retention 30 gün (debug + audit için)

## 4. Zanzibar plane özel koruma (Codex)

### OpenFGA store/model authority

- **Vault authoritative**: `kv/platform/openfga` → store_id + model_id
- **platform-k8s-gitops ExternalSecret**: `kustomize/base/apps/permission-service/ops/externalsecret.yaml` (Vault mount)
- **platform-backend sadece kaynak**: DSL (model.fga) + Java client kodu; runtime model-id bump iddiası YOK

### Fixture/runbook sınırı

- **Fixtures test-only** → platform-backend `openfga/tuples-seed.json`
- **Ops runbook canonical** → platform-k8s-gitops `docs/prod-scoped-allow-seed-runbook.md`
- **Disiplin**: fixtures'ta prod token/secret yok; runbook'ta secret kaynağı = Vault/env

### Common-auth koordinat stabilite

- groupId: `com.example`
- artifactId: `common-auth`
- version: `1.1.0`
- **Relocation POM gerek YOK** (1:1 migration, hiçbir package rename)
- 19.2'deki servisler (auth/user/variant) compile-time bağımlılık korunur

## 5. D29 authz kanıtı

| Katman | Durum | Kanıt |
|---|---|---|
| Up | ✓ Faz 18 evidence | K8s platform-prod 19 Running (permission-service dahil) |
| Functional | ✓ Faz 18.5-18.7 | `/api/` 401 JWT flow (API gateway + auth chain) |
| Zanzibar-ready | Kısmi ✓ Faz 18 | `/api/v1/authz/version` 401 "JWT token zorunludur" (authz chain alive, store/model parity) |
| Zanzibar-ready synthetic | Faz 19.9'a bırakıldı | Allow (200) + deny (403) sentetik cutover öncesi |

**CI DSL validate auxiliary kanıt**, authoritative değil. Canlı D29 cutover zamanı (19.9).

## 6. 19.3 exit criteria (Codex)

1. ✓ platform-backend CI permission-service + common-auth derleniyor (bu PR)
2. ✓ OpenFGA DSL CI'da syntax+parse+JSON transform validate
3. ✓ D29 Zanzibar-ready kısmen mevcut (401 JWT) + synthetic Faz 19.9
4. ✓ Fixture/runbook sınırı UPHELD (ADR-0004'te net)

## 7. Zero regression

- platform-k8s-gitops + platform-ssot UNCHANGED
- K8s 19+10+11 Running unchanged
- OpenFGA Vault kv/platform/openfga ExternalSecret path UNCHANGED
- Edge 200

## 8. Next

Faz 19.4+19.5 backend batch 3+4 combined — CI batch scope full coverage:
- batch 3: core-data-service + report-service + schema-service
- batch 4: api-gateway + discovery-server

Sonra Faz 19.6 platform-web migration (filter-repo post-process + CI + hijyen).

## Referans

- ADR-0004 split-repo authority transfer
- PLAN.md §Faz 19
- Codex thread `019dc0d8` (19.3 AGREE)
- platform-backend PR #2
- Faz 18.5-18.7 evidence: `docs/phase18-evidence/faz-18-5-7-complete-20260424.md`
- Scoped allow seed runbook: `docs/prod-scoped-allow-seed-runbook.md`
