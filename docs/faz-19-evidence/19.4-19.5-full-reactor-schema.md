# Faz 19.4+19.5 — Full Reactor + schema-service Standalone (Backend Batch 3+4)

**Date:** 2026-04-24
**Scope:** platform-backend backend batch 3+4 combined CI extend.
**Codex AGREE:** thread `019dc0cc` + `019dc0d8` (Faz 19.2+19.3 patterns applied).

---

## 1. Scope

### Faz 19.4 — Backend batch 3
- **core-data-service** (parent pom)
- **report-service** (parent pom)
- **schema-service** (standalone, NOT in parent pom)

### Faz 19.5 — Backend batch 4
- **api-gateway** (parent pom)
- **discovery-server** (parent pom, legacy D7 marker)

**Combined rationale:** İki batch de sadece CI scope genişletme; kod değişikliği yok. Codex pattern (19.2 PR-A) tek PR yeterli.

## 2. Combined CI workflow

**Değişiklikler (platform-backend PR #3):**

### batch-build → full-reactor-build

- `-pl auth-service,user-service,variant-service,permission-service,common-auth -am` batch scope **kaldırıldı**
- `./mvnw -q -DskipTests verify` (parent pom 9 modül full reactor)
- Timeout 20→30 dakika

### Yeni job `schema-service-build`

- `schema-service` parent pom'da değil → ayrı build
- `cd schema-service && ../mvnw verify`
- Timeout 10 dakika

### openfga-dsl-check

Değişmedi (basic structural check).

## 3. 10 module coverage

| Service | Repo Location | Parent POM? | CI Job |
|---|---|---|---|
| auth-service | `auth-service/` | ✓ | full-reactor-build |
| user-service | `user-service/` | ✓ | full-reactor-build |
| variant-service | `variant-service/` | ✓ | full-reactor-build |
| core-data-service | `core-data-service/` | ✓ | full-reactor-build |
| report-service | `report-service/` | ✓ | full-reactor-build |
| permission-service | `permission-service/` | ✓ | full-reactor-build |
| common-auth | `common-auth/` | ✓ | full-reactor-build |
| api-gateway | `api-gateway/` | ✓ | full-reactor-build |
| discovery-server | `discovery-server/` | ✓ (legacy) | full-reactor-build |
| schema-service | `schema-service/` | ✗ (standalone) | schema-service-build |

## 4. User direktif uyum

### "discovery service i almayı unutma"

✓ UPHELD:
- Kaynak kod platform-backend `discovery-server/` (parent pom modülü)
- CI full-reactor-build compile + verify
- Runtime D7 kaldırılmış ama kaynak kod erişilebilir

### "Kaynak raporu tek amacı: geliştirme taşıma"

✓ UPHELD:
- Tüm backend services platform-backend'e taşındı
- Ssot kaynağı olarak kalıyor (Faz 19.10'da archive)

### Codex default'lar (thread 019dc0ac)

- Repo count 2 ✓ (backend + web)
- Naming ✓ (platform-backend + platform-web)
- Path-filtered full history ✓ (sha-map saved 19.1)
- Dual-build transition planned 19.8
- Option A (K8s frontend) DEFERRED 19.10+
- Reports data contract gitops'ta ✓ (docs/migration/*)

## 5. Exit criteria

Platform-backend PR #3 merge sonrası:
- [ ] full-reactor-build GREEN (9 parent modül)
- [ ] schema-service-build GREEN (standalone)
- [ ] openfga-dsl-check GREEN (basic)
- [ ] Backend repo 10 module CI coverage (auth + user + variant + core + report + schema + permission + common-auth + gateway + discovery) 

Runtime kanıt (K8s unchanged):
- 19 prod + 10 test Running
- Edge 200
- OpenFGA Vault auth unchanged

## 6. Zero regression

- platform-k8s-gitops + platform-ssot UNCHANGED
- Runtime manifest gitops authority (image ref `platform-ssot-*` korundu 19.8'e kadar)

## 7. Next (Faz 19.6)

Platform-web migration:
- Filter-repo aftermath cleanup (large .next/cache files >50MB)
- CI workflow (npm/pnpm install + lint + test + build)
- CODEOWNERS + CONTRIBUTING + PR template
- README update (live status + MFE list + design-system)
- Legacy workflow disable (backend repo pattern)

## Referans

- ADR-0004 split-repo authority transfer
- PLAN.md §Faz 19
- Codex thread 019dc0cc (19.2 plan)
- Codex thread 019dc0d8 (19.3 Zanzibar plan)
- platform-backend PR #3 (19.4+19.5 combined)
