# Faz 19.6 — Web CI Workflow + Repo Hygiene (platform-web PR #12)

**Date:** 2026-04-24
**Scope:** platform-web ilk governance PR (CI workflow + 16 legacy workflow disable + CODEOWNERS + CONTRIBUTING + README).
**Codex AGREE:** thread `019dc0ac` (Faz 19 detaylı 10-step).

---

## 1. platform-web PR #12

URL: https://github.com/Halildeu/platform-web/pull/12

**Değişiklikler:**

### `.github/workflows/ci-web-check.yml` (yeni)

- Trigger: push main + pull_request main + workflow_dispatch
- Node 22.12 + pnpm 10.12.4 (root package.json packageManager pin)
- Cache pnpm store
- `pnpm install --frozen-lockfile`
- `pnpm run -w lint` (baseline tolerate, 19.8+'da tighten)
- Timeout 15 dakika

### 16 legacy workflow → `workflows-legacy/`

Tüm existing workflows disabled (case-by-case 19.8+'da re-enable):
- audit-quality, benchmark-gate, chromatic, codeql, compatibility-matrix
- design-system-doctor, design-system-gate, i18n-smoke, npm-publish, release
- scorecard-gate, secret-scan, security-guardrails, stale, token-drift-check, ui-kit-ci

### CODEOWNERS + CONTRIBUTING + README

- `.github/CODEOWNERS` → `@Halildeu` solo pattern
- `CONTRIBUTING.md` rewrite (repo sınırı + Option B canonical + pnpm workflow + large file note)
- `CONTRIBUTING-LEGACY.md` preserved historical
- `README.md` update (live status + 739 commit + Option B)

## 2. Frontend delivery — Option B canonical (Faz 18.11.a UPHELD)

- `ai.acik.com` → K8s prod NodePort `127.0.0.1:30443`
- `testai.acik.com` → K8s test NodePort `127.0.0.1:31080` + `127.0.0.1:5545`
- Edge: host-static `platform-web-nginx` (prod) + `platform-web-nginx-stage` (test)
- **K8s frontend authoritative DEĞİL** (Option A Faz 19.10+ karar kapısı)
- platform-web repo sadece source + build

## 3. Large file warning (19.1 filter-repo'dan kalma)

2 dosya >50MB GitHub warning (push başarılı):
- `archive/docs-nextra/.next/cache/webpack/client-production/0.pack` (81.67 MB)
- `apps/docs/.next/cache/webpack/client-production/0.pack` (72.03 MB)

Eski build artefaktları (2026-03-21/29 history). **Faz 19.8+'da** değerlendirme:
- BFG Repo-Cleaner history rewrite
- Veya `git filter-repo --strip-blobs-bigger-than 50M`
- Veya bırak (history audit için), sadece future pushes için .gitignore

## 4. Zero regression

- platform-k8s-gitops + platform-ssot UNCHANGED
- Runtime manifest gitops authority (image ref `platform-ssot-frontend` korundu 19.8'e kadar)
- Edge 200 her iki domain (K8s NodePort routing)

## 5. Sıradaki (Faz 19.7)

Reports code split — docs-only (data contract gitops'ta zaten):
- `mfe-reporting` → platform-web `apps/mfe-reporting/` (19.1 migrated) ✓
- `report-service` → platform-backend `report-service/` (19.1 migrated + 19.4 CI coverage) ✓
- **Data contract gitops authority:**
  - `docs/migration/mssql-pg-data-contract.md` ✓
  - `docs/migration/report-source-annex.yaml` ✓
  - `docs/migration/schema-introspection-annex.yaml` ✓
  - `docs/migration/flyway-v16-plan.md` ✓

Codex direktif: "Reports code taşınır, data contract gitops'ta DRAFT annex korunur" ✓ UPHELD.

## Referans

- ADR-0004 split-repo authority transfer
- PLAN.md §Faz 19
- Codex thread `019dc0ac`
- platform-web PR #12
- Faz 18.11.a frontend canonical (Option B host-static)
