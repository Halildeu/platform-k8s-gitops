# RB Faz 22.5 — #1359 Acceptance Evidence Template (Endpoint Agent tokenless AutoEnroll DNS/edge mTLS host activation)

> **Status**: SOURCE DRAFT
> **Runtime mutation**: NONE
> **Operator gate**: REQUIRED (DNS + server cert/key + client CA + AD CS Web Enrollment + valid machine cert)
> **Closure claim**: NO (source-side draft; acceptance evidence pack generated by operator + agent collectors)
> **Tracked by**: [#1359](https://github.com/Halildeu/platform-k8s-gitops/issues/1359) Endpoint Agent tokenless AutoEnroll DNS/edge mTLS host activation
> **Evidence template**: bu doc operator post-activation acceptance evidence bundle iskeleti
> **Codex thread**: `019ea922` plan-time AGREE (single-PR scope, pattern from RB-faz22.3-edge-mtls-autoenroll.md)
> **HARD RULE Tam Otonom**: operator-gate dependency D1 (DNS) + D2 (AD CS) + D3 (client CA) + M2 (#1376) prerequisite — bu template `Closure claim: NO`; source-side LIVE iddiası `#1385` consensus gate tracker'a bağlı

---

## 1. Boundary

Bu template **#1359 closure acceptance evidence bundle** için iskelet. Source-side LIVE artifact'lar:
- `docs/runbooks/RB-faz22.3-edge-mtls-autoenroll.md` (canonical edge mTLS host runbook)
- `docs/adr/0029-faz22-mass-deployment-mtls-msi-gpo.md` (Plan A mass deployment context)
- `kustomize/base/edge-mtls/` (host-edge template — varsa)
- `scripts/faz22-mass-deployment/ad-cs-preflight.ps1` (AD CS preflight)

Runtime acceptance gate (operator-bound):
- DNS A record `<autoenroll-edge>.acik.com` resolved
- Server cert/key path mounted (Vault stdin-pipe seed pattern)
- Client CA bundle deployed (`/etc/nginx/ssl/client-ca-bundle.pem`)
- AD CS Web Enrollment LIVE (M2 #1376 prerequisite)
- Valid machine cert end-to-end issuance (agent → CA → certreq → SAN URI:adcomputer:{objectGUID})
- Edge mTLS server-side acceptance + spoof negative + no-cert negative PASS

## 2. Evidence Bundle Layout (operator collects + agent helpers)

```
evidence/1359-autoenroll-edge-activation-YYYYMMDD/
├── 01-dns-resolution.txt           # dig +short <host>.acik.com propagation
├── 02-edge-cert-mount.txt          # docker exec edge openssl x509 -in /etc/nginx/ssl/server.crt -noout -text
├── 03-client-ca-bundle.txt         # docker exec edge openssl verify -CAfile /etc/nginx/ssl/client-ca-bundle.pem self-test
├── 04-adcs-web-enrollment.txt      # certutil -ping + Web Enrollment URL HTTP 200
├── 05-machine-cert-issuance.txt    # certreq -submit -config <CA>\Issuer <csr> output + SAN URI:adcomputer:{objectGUID}
├── 06-edge-mtls-positive-smoke.txt # curl --resolve + --cert + --key valid PASS (HTTP 200 backend response)
├── 07-edge-mtls-nocert-negative.txt # curl --resolve no --cert → HTTP 4xx (TLS handshake reject)
├── 08-edge-mtls-spoof-negative.txt  # curl --resolve + --cert + X-Forwarded-Client-Cert spoof header → reject
├── 09-agent-autoenroll-flow.txt    # endpoint-agent.exe --auto-enroll log capture (one test PC end-to-end)
├── 10-backend-enrollment-record.txt # backend endpoint-admin-service /enrollments query → 1 new record
└── README.md                       # bundle context (date, operator, AD CS CA name, edge host, agent version)
```

## 3. Evidence Section Templates

### 3.1 DNS Resolution (`01-dns-resolution.txt`)

```bash
# Operator runs (corp network):
dig +short <autoenroll-edge>.acik.com
# Expected: IP address (single A record); propagation ≥10 min

# Optional: cross-region check
dig @8.8.8.8 +short <autoenroll-edge>.acik.com
dig @1.1.1.1 +short <autoenroll-edge>.acik.com

# Agent helper script:
scripts/faz22-mass-deployment/check-dns-propagation.sh <autoenroll-edge>.acik.com
# (TODO: create if not exists — D1 organize path)
```

### 3.2 Edge Cert Mount (`02-edge-cert-mount.txt`)

```bash
# Operator (staging-sw):
docker exec staging-edge openssl x509 -in /etc/nginx/ssl/server.crt -noout -dates -subject -issuer
# Expected: notBefore/notAfter valid window; subject CN=<autoenroll-edge>.acik.com; issuer=corp CA

# Cert chain trust:
docker exec staging-edge openssl verify -CAfile /etc/nginx/ssl/corp-root-ca.pem /etc/nginx/ssl/server.crt
# Expected: OK
```

### 3.3 Client CA Bundle (`03-client-ca-bundle.txt`)

```bash
# Operator:
docker exec staging-edge openssl x509 -in /etc/nginx/ssl/client-ca-bundle.pem -noout -subject -issuer
# Expected: subject=corp Enterprise CA; issuer=corp Root CA

# Verify chain:
docker exec staging-edge openssl verify -CAfile /etc/nginx/ssl/corp-root-ca.pem /etc/nginx/ssl/client-ca-bundle.pem
# Expected: OK

# Size sanity:
docker exec staging-edge wc -c /etc/nginx/ssl/client-ca-bundle.pem
# Expected: >1KB (full chain); single-cert ~1.5KB; multi-CA bundle 3-10KB
```

### 3.4 AD CS Web Enrollment (`04-adcs-web-enrollment.txt`)

```powershell
# Operator (DC or admin PC):
certutil -ping
# Expected: CA certutil-ping responds; no error

# Web Enrollment URL probe:
$caUrl = "https://<adcs-host>/certsrv"
curl.exe -k -I $caUrl
# Expected: HTTP 401 (authentication required) or HTTP 200 (anonymous landing); NOT 404/500

# Web Enrollment template visible:
# Operator browser: $caUrl → Request a Certificate → advanced certificate request → templates list
# Expected: "Machine" / "EndpointAgentCert" / "Workstation" template visible per AD CS preflight
```

### 3.5 Machine Cert Issuance (`05-machine-cert-issuance.txt`)

```powershell
# Operator (test PC, domain-joined):
# 1. CSR generate:
$csr = "<temp-machine-cert.csr>"
certreq -new $csrPolicy $csr

# 2. Submit:
certreq -submit -config "<CA-host>\<CA-name>" $csr <out-cert>

# 3. Install:
certreq -accept <out-cert>

# 4. Verify cert in MY (LocalMachine) store:
Get-ChildItem Cert:\LocalMachine\My | Where-Object Subject -like "*<host>*" | Format-List Subject, Issuer, NotAfter, Thumbprint

# 5. SAN URI:adcomputer:{objectGUID} extract:
$cert = Get-ChildItem Cert:\LocalMachine\My | Where-Object Subject -like "*<host>*"
$cert.Extensions | Where-Object {$_.Oid.FriendlyName -eq "Subject Alternative Name"} | ForEach-Object { $_.Format(1) }
# Expected: URI=adcomputer:{<actual-AD-objectGUID>}
```

### 3.6 Edge mTLS Positive Smoke (`06-edge-mtls-positive-smoke.txt`)

```bash
# Operator (test PC with valid machine cert):
# Export cert + key for curl (PFX → PEM):
openssl pkcs12 -in machine-cert.pfx -clcerts -nokeys -out machine.crt
openssl pkcs12 -in machine-cert.pfx -nocerts -nodes -out machine.key

# Positive smoke:
curl --resolve "<autoenroll-edge>.acik.com:443:<edge-IP>" \
     --cert machine.crt --key machine.key \
     https://<autoenroll-edge>.acik.com/api/v1/endpoint-agent/endpoint-enrollments/auto

# Expected: HTTP 200 OR 201 (backend endpoint-admin-service /enrollments/auto response)
# Body: {"agentId": "...", "enrollmentToken": "...", "ttl": ...}
```

### 3.7 No-Cert Negative (`07-edge-mtls-nocert-negative.txt`)

```bash
# Operator (test PC, no client cert):
curl --resolve "<autoenroll-edge>.acik.com:443:<edge-IP>" \
     -v https://<autoenroll-edge>.acik.com/api/v1/endpoint-agent/endpoint-enrollments/auto 2>&1 | head -30

# Expected: TLS handshake fail (SSL_R_TLSV13_ALERT_CERTIFICATE_REQUIRED or "no client certificate sent")
# NOT: HTTP 401/403/404 (would indicate edge passes through to backend without cert verify)
```

### 3.8 Header Spoof Negative (`08-edge-mtls-spoof-negative.txt`)

```bash
# Operator (test PC, attempt to spoof client cert header):
curl --resolve "<autoenroll-edge>.acik.com:443:<edge-IP>" \
     --cert machine.crt --key machine.key \
     -H "X-Forwarded-Client-Cert: CN=attacker-injected" \
     -H "X-Client-Cert-Subject: CN=attacker-injected" \
     -v https://<autoenroll-edge>.acik.com/api/v1/endpoint-agent/endpoint-enrollments/auto 2>&1 | head -30

# Expected: edge strips spoofed headers; backend receives only real cert subject (from edge proxy header set)
# Backend log evidence: cert subject == actual machine cert SAN URI:adcomputer:{objectGUID}, NOT "CN=attacker-injected"

# Alternative spoof tests:
# - Empty cert + spoof header → 401/403
# - Wrong header case (x-Forwarded-client-Cert) → edge case-insensitive strip
```

### 3.9 Agent AutoEnroll Flow (`09-agent-autoenroll-flow.txt`)

```bash
# Operator (test PC, Endpoint Agent installed — Windows service: EndpointAgent;
# binary: %ProgramFiles%\EndpointAgent\endpoint-agent.exe; NOT PlatformAgent):

# 1. Reset auto-enroll state to force a fresh tokenless enroll (PowerShell):
Stop-Service EndpointAgent
Remove-Item -LiteralPath "$env:ProgramData\EndpointAgent\config\auto-enroll.dpapi" -Force -ErrorAction SilentlyContinue
#   NOTE: the auto-enroll runner persists to auto-enroll.dpapi (internal/platform/windows/dpapi);
#   the HMAC/manual-token path uses a SEPARATE hmac-credential.dpapi (internal/hmacstore).
#   There is NO state.json. For an auto-enroll reset, remove auto-enroll.dpapi.

# 2. Trigger auto-enroll with real flags only (run `endpoint-agent.exe --help` for the full set;
#    --log-level / --log-file do NOT exist; the service writes logs to %ProgramData%\EndpointAgent\logs):
& "$env:ProgramFiles\EndpointAgent\endpoint-agent.exe" --auto-enroll --api-url "https://<autoenroll-edge>.acik.com/api/v1/endpoint-agent"
#   --api-url is the full canonical base; the wire client appends /endpoint-enrollments/auto, so the
#   request lands on POST /api/v1/endpoint-agent/endpoint-enrollments/auto (gateway route parity #1357).
#   Add --dry-run to validate cert + TLS + persisted-config WITHOUT any HTTP call (exit code = smoke result).

# 3. Inspect the service log for the auto-enroll chain. Wording below is illustrative —
#    the exact strings come from internal/autoenroll/runner.go and may evolve:
# - "auto-enroll cert loaded: subject=... thumbprint_sha256=... not_after=..."
# - "auto-enroll reissue: device_id=... existing=... expires_at=..."
# Machine-checkable evidence (preferred over a log-string grep):
# - %ProgramData%\EndpointAgent\config\auto-enroll.dpapi present after the run
# - backend enrollment record created (cross-check via §3.10)
```

### 3.10 Backend Enrollment Record (`10-backend-enrollment-record.txt`)

```bash
# Operator (admin JWT):
TOKEN="<admin-jwt>"
curl -sS -H "Authorization: Bearer $TOKEN" \
     "https://<backend-host>/api/v1/endpoint-admin/enrollments?subject=<corp-host>" | jq .

# Expected response includes:
# - 1 new enrollment record
# - source: "auto-enroll"
# - cert_subject_san_uri: "URI:adcomputer:{<actual-AD-objectGUID>}"
# - created_at: <recent timestamp>
# - agent_id: <uuid>
# - status: "active"

# Cross-check via OpenFGA tuple:
# Optional: permission-service check_subscription "agent:<agentId>" → active
```

## 4. Acceptance Closure Checklist (operator + agent)

- [ ] §3.1 DNS resolution PASS (≥10 min propagation)
- [ ] §3.2 Edge server cert mount + chain trust PASS
- [ ] §3.3 Client CA bundle deployed + self-verify PASS
- [ ] §3.4 AD CS Web Enrollment LIVE + template visible
- [ ] §3.5 Machine cert end-to-end issuance + SAN URI:adcomputer:{objectGUID} valid
- [ ] §3.6 Edge mTLS positive smoke HTTP 200 + backend enrollment created
- [ ] §3.7 No-cert negative TLS handshake reject (NOT HTTP-level fail)
- [ ] §3.8 Header spoof negative — edge strips spoofed headers; backend receives real cert subject
- [ ] §3.9 Agent --auto-enroll flow log chain complete + auto-enroll.dpapi persisted
- [ ] §3.10 Backend enrollment record present + SAN URI cross-check PASS
- [ ] Evidence bundle archived to `evidence/1359-autoenroll-edge-activation-YYYYMMDD/`
- [ ] Mavis ops sign-off comment on #1359
- [ ] Source PR ref (this template + tracker #1375) → board comment cross-link

## 5. Closure Provenance

Cross-AI peer review:
- Implementer: Claude (Anthropic) — Session 51 Faz 22 otonom chain (single-PR scope)
- Reviewer (plan-time): Codex (OpenAI GPT-5.2) thread `019ea922` AGREE pattern (RB-faz22.3-edge-mtls-autoenroll.md inspiration)
- Verdict: AGREE source-side draft + 10-section evidence template iskelet

**Closure ≠ template merge**: Bu PR template MERGED ≠ #1359 closed. Closure operator collects all 10 sections + Mavis ops sign-off + acceptance closure checklist 11/11 PASS sonra.
