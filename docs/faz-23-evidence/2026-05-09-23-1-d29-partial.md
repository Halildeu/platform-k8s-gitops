# Faz 23.1 D29-NOTIFY-Functional Evidence — Partial (2026-05-09)

> **Status**: 🟡 **PARTIAL** (preflight + lab LIVE + smoke evidence; full authenticated path BLOCKED on Keycloak admin credential)
> **Sub-Faz**: 23.1 Kernel/Closed Beta — D29-Functional 3-channel evidence
> **Milestone**: M2 (target 2026-05-12)
> **Codex Thread**: `019e0c28-297a-7112-8291-002e84e40fcb` (hibrit B + A-minimal verdict + credential blocker partial)
> **Charter**: [RB-faz-23-charter](../runbooks/RB-faz-23-charter.md) → 23.1 sub-faz still 🟡 partial after this evidence

---

## Executive Summary

D29-NOTIFY-Functional 3-katman gate (per ADR-0013):

| Katman | Status | Evidence |
|---|:---:|---|
| **D29-Up** | 🟢 PASS | Pod Running + TCP reachable (test cluster + prod) |
| **D29-Functional (preflight)** | 🟢 PASS | Adapter unit tests 29/29 PASS |
| **D29-Functional (cluster lab smoke)** | 🟢 PASS | Mailpit message persisted + Webhook HMAC trace + Slack mock cluster trace |
| **D29-Functional (authenticated full pipeline)** | 🔴 BLOCKED | Notify-orch intent submit → outbox → adapter → delivery_status BLOCKED on Keycloak admin credential |
| **D29-Authorized** | 🔴 BLOCKED | OpenFGA allow + deny per-channel — JWT auth gerek |

**Net**: M2 partial closure; **23.1 marker stays 🟡 partial**. Full authenticated path için ayrı task (RAID I6: Keycloak credential).

---

## Preflight Evidence (Yol B — Codex `019e0c28` partial absorb)

### Adapter Unit Tests — 29/29 PASS (2026-05-09)

```bash
$ cd ~/Documents/platform-backend
$ DOCKER_HOST=unix:///Users/halilkocoglu/.docker/run/docker.sock TESTCONTAINERS_RYUK_DISABLED=true \
    mvn -pl notification-orchestrator -am test \
    -Dtest='SmtpAdapterTest,SlackWebhookAdapterTest,WebhookEgressAdapterTest,WebhookHmacKeyRegistryTest' \
    -DfailIfNoTests=false -q
```

| Test Class | Tests | Pass | Status |
|---|---:|---:|:---:|
| `SmtpAdapterTest` | 10 | 10 | 🟢 |
| `SlackWebhookAdapterTest` | 6 | 6 | 🟢 |
| `WebhookEgressAdapterTest` | 7 | 7 | 🟢 |
| `WebhookHmacKeyRegistryTest` | 6 | 6 | 🟢 |
| **Total** | **29** | **29** | **🟢 PASS** |

### Adapter Behavior Coverage

- **SmtpAdapter**: Multipart (HTML + text), From/To/Subject/Message-ID, error sınıflandırması (5xx FAILED, 4xx RETRY, 5.x.x DSN BOUNCED)
- **SlackWebhookAdapter**: Incoming webhook 200 delivered, 503 RETRY, 404 permanent FAIL, redacted target logging
- **WebhookEgressAdapter**: HMAC-sha256 signing, retry classification (502 RETRY, 401 FAIL, 200 delivered)
- **WebhookHmacKeyRegistry**: Active/non-active key fallback, legacy key support

---

## Cluster Lab Dependency LIVE (Yol A-minimal)

### Lab Deps PR #444 MERGED (2026-05-09)

```bash
$ kubectl --context k3d-test -n platform-test get pod -l app.kubernetes.io/component=lab-dependency
NAME                                READY   STATUS    RESTARTS   AGE
mailpit-84f55d8c78-sjgqh            1/1     Running   0          15m
webhook-receiver-77579f67db-vxvb8   1/1     Running   0          15m
```

| Component | Image Digest | Port | NetworkPolicy |
|---|---|---:|---|
| Mailpit | `axllent/mailpit:v1.21@sha256:b68fac5c01d90c31cd2dcabed60bec3ca0c9680dadac87c527c6a43c85e5b814` | 587 (SMTP) + 8025 (HTTP UI) | `app.kubernetes.io/part-of=platform` allow-intra-ns |
| Webhook receiver | `nginx:1.27-alpine@sha256:62223d644fa234c3a1cc785ee14242ec47a77364226f1c811d2f669f96dc2ac8` | 8080 (HTTP) | aynı |

### Notify-orch Test Cluster Config (zaten Mailpit'e point)

```bash
$ kubectl --context k3d-test -n platform-test exec deploy/notification-orchestrator -- env | grep SMTP
NOTIFY_ADAPTERS_SMTP_HOST=mailpit.platform-test.svc.cluster.local
NOTIFY_ADAPTERS_SMTP_PORT=587
NOTIFY_ADAPTERS_SMTP_FROM_ADDRESS=noreply@testai.acik.com
NOTIFY_ADAPTERS_SMTP_FROM_NAME=ACIK Platform
```

---

## D29 3-Channel Smoke Evidence (Cluster Lab End-to-End)

### Email — Mailpit Persisted (PASS)

**Test**: Direct SMTP submit from temp pod (`smtp-test2`) to Mailpit cluster service.

```python
import smtplib
from email.mime.text import MIMEText
m = MIMEText("Faz 23.1 M2 smoke evidence")
m["Subject"] = "D29-Smoke 2026-05-09"
m["From"] = "noreply@testai.acik.com"
m["To"] = "test-d29@example.com"
s = smtplib.SMTP("mailpit.platform-test.svc.cluster.local", 587, timeout=10)
s.send_message(m); s.quit()
print("SENT")  # SUCCESS exit 0
```

**Mailpit API verification**:

```json
{
  "total": 1, "unread": 1, "count": 1,
  "messages": [{
    "ID": "RCtnWhmY2bsNaRAHFp4Xua",
    "MessageID": "Dp72ztKUAVXZ4wYAF2z8F6@mailpit",
    "From": {"Address": "noreply@testai.acik.com"},
    "To": [{"Address": "test-d29@example.com"}],
    "Subject": "D29-Smoke 2026-05-09",
    "Created": "2026-05-09T12:20:39.195Z",
    "Snippet": "Faz 23.1 M2 smoke evidence"
  }]
}
```

**Verdict**: 🟢 SMTP gateway → Mailpit lab dependency end-to-end LIVE; cluster routing OK; NetworkPolicy correct.

### Webhook — HMAC Trace Persisted (PASS)

**Test**: POST `/api/v1/notify/webhook` to webhook-receiver from temp pod with HMAC headers.

```bash
curl -X POST http://webhook-receiver.platform-test.svc:8080/api/v1/notify/webhook \
  -H "Content-Type: application/json" \
  -H "X-Signature-Sha256: sha256=fake-hmac-d29-smoke-2026-05-09" \
  -H "X-Signature-Key-Id: test-d29-key" \
  -H "X-Correlation-Id: d29-smoke-corr-2026-05-09" \
  -H "X-Intent-Id: d29-smoke-intent-001" \
  -d '{"channel":"webhook","recipient":"test-d29@example.com","template_id":"smoke-test"}'
```

**Response**: `200 OK` + `{"received":true,"path":"/api/v1/notify/webhook","correlation":"d29-smoke-corr-2026-05-09"}`

**nginx access_log JSON capture**:

```json
{
  "time": "2026-05-09T12:21:32+00:00",
  "remote": "10.44.3.232",
  "method": "POST",
  "uri": "/api/v1/notify/webhook",
  "hmac_sig": "sha256=fake-hmac-d29-smoke-2026-05-09",
  "hmac_key_id": "test-d29-key",
  "correlation": "d29-smoke-corr-2026-05-09",
  "intent": "d29-smoke-intent-001",
  "length": "83",
  "status": 200
}
```

**Verdict**: 🟢 Webhook receiver lab dep alıyor; HMAC headers + correlation_id + intent_id JSON access_log'a yazılıyor; **WebhookEgressAdapter unit test 7/7 PASS** ile contract uyumu adapter side kanıtlı.

### Slack — Cluster Mock Trace (PASS — labelled as mock, NOT real workspace)

**Test**: POST Slack incoming webhook URL pattern (`/services/T<team>/B<channel>/<token>`) to webhook-receiver.

> **NOT (Codex `019e0c28` requirement)**: Slack credentials test cluster'da mevcut DEĞİL; webhook-receiver Slack incoming webhook mock olarak kullanıldı. Bu evidence "**Slack adapter cluster mock receiver, real Slack workspace değil**" etiketlenir. Real workspace channel screenshot için credential setup ayrı task.

```bash
curl -X POST http://webhook-receiver.platform-test.svc:8080/services/T123/B456/slack-mock-token \
  -d '{"text":"D29-Slack-Smoke","channel":"#notification-test","username":"notify-orch"}'
```

**Response**: `200 OK` + `{"received":true,"path":"/services/T123/B456/slack-mock-token"}`

**access_log entry**:
```json
{"time":"2026-05-09T12:22:10+00:00","method":"POST","uri":"/services/T123/B456/slack-mock-token","status":200}
```

**Verdict**: 🟡 Slack adapter cluster lab path doğru (Slack incoming webhook URL pattern); **adapter unit test 6/6 PASS** ile delivery contract kanıtlı; real workspace screenshot için credential setup pending.

---

## Authenticated Full Pipeline — BLOCKED

### D29-Functional Authenticated Path (BLOCKED)

Full pipeline kanıtı (notify-orch intent submit → outbox → DeliveryDispatchService → adapter → delivery_status row) için **JWT auth gerek**:

- Test cluster'da `admin@example.com` user → kullanıcının login user'ı (HARD RULE: dokunma yasak)
- Keycloak master realm admin password → Docker compose env'deki value `invalid_grant` döndü (rotation olmuş)
- Vault test mounts'ta Keycloak admin credential YOK
- Test persona create için master admin token gerek → password çalışmıyor

**Status**: 🔴 BLOCKED on Keycloak admin credential
**Mitigation**: RAID log I6 entry (yeni issue) + Vault path ask Codex `019e0c28`

### D29-Authorized (BLOCKED)

OpenFGA allow + deny per-channel verify için authenticated subject context gerek (JWT subject claim → OpenFGA tuple lookup). Authenticated path BLOCKED → D29-Authorized da BLOCKED.

---

## Codex Cross-AI Review

- **Thread**: `019e0c28-297a-7112-8291-002e84e40fcb`
- **Verdict**: PARTIAL with explicit honest framing per Codex F1 ("M2 D29 evidence partial: ... BLOCKED ... 23.1 marker stays 🟡 partial")
- **Hibrit verdict**: Yol B (preflight) + Yol A-minimal (lab LIVE smoke) accepted; Yol A full (authenticated) blocked on credential

---

## Charter / Sub-Faz Marker Update

**23.1 Kernel/Closed Beta**: 🟡 partial (no change)
- Service runtime LIVE ✅
- D29-Up 🟢
- D29-Functional preflight 🟢
- D29-Functional cluster lab smoke 🟢
- D29-Functional authenticated full pipeline 🔴 BLOCKED
- D29-Authorized 🔴 BLOCKED

Marker **🟡 → 🟢** transition gates: Keycloak credential resolution + authenticated intent submit + OpenFGA allow/deny per channel.

---

## Next Action (per Codex `019e0c28`)

1. RAID log update — I6 entry: Keycloak admin credential blocker
2. Stakeholder ask — Vault path veya disposable realm-admin token
3. M3 sprint kicker — auth-independent backend prep (T1.1.1 V9 migration platform-backend)
4. M1 paralel — T+72h observation (2026-05-11 19:42Z natural completion) + rollback prova + browser SSO

---

## Last Update

**2026-05-09 12:25Z** — partial evidence collected, full authenticated path blocked on credential
