# V2.1 Exit #4 — Perf Alert Receiver Teams Primary LIVE (2026-05-28)

> **ADR-0029 Hibrit D PR-3b operator activation chain COMPLETE**
> **Cross-AI peer review**: Codex thread `019e6bd1` (4-iter AGREE chain — D43 SMTP dormant + V2.1 Teams unblock)
> **Implementer**: Claude (Anthropic) / **Reviewer**: Codex (OpenAI GPT-5.2)
> **Date**: 2026-05-28 ~02:30 UTC live execution; Session 51 closure batch

## 1. Bağlam

Faz 23 V2.1 Ops-A perf-alert receiver pivot: Microsoft Teams Power Automate workflow + Adaptive Card primary canlı path (ADR-0029 Hibrit D). PR-1 (docs) + PR-2 (helm/ESO source-side desired) + PR-3a (runbook V2.1 sweep) MERGED 2026-05-27. PR-3b operator activation 2026-05-28 LIVE — cluster Alertmanager → Power Automate → Teams Adaptive Card end-to-end functional receipt.

Intersection blocker: **BL-008 pre-existing D43 SMTP receiver `auth_*_file` schema gap** (board #854). Operator v0.90.1 reject `auth_username_file`/`auth_password_file` → Alertmanager config sync FAIL → mevcut Alertmanager pod 41 günlük eski config'le çalışıyor, yeni `perf-alertmanager-teams-secrets` mount edilmiyor. PR #1099 (Codex `019e6bd1` AGREE 4-iter chain) D43 SMTP `email_configs` block explicit dormant — receiver name-only intentional empty; route `continue: true` preserved → `alarm-receiver-bridge` GitHub Issue path active kalır (defense-in-depth). Helm REVISION 4 + Alertmanager pod recreate sonrası Teams receiver active.

## 2. Mimari (Hibrit D — acik tenant)

```
PerfPrometheusRule (team=perf labels)
        ↓
Alertmanager route: team=perf → perf-alerts-teams continue:true + alarm-receiver-bridge sibling
        ↓                                              ↓
perf-alerts-teams receiver                    alarm-receiver-bridge
  webhook_configs:                              webhook_configs:
    url_file: /etc/.../TEAMS_WEBHOOK_URL          url: http://alertmanager-bridge:9093/alert
        ↓                                              ↓
Power Automate HTTP trigger                    GitHub Issue (bridge service)
        ↓
Microsoft Teams connector:
  Post adaptive card in chat/channel
  Takım: Denetim
  Kanal: #perf-alerts (Özel/Private, Halil-Serban tenant)
        ↓
Teams Adaptive Card render:
  - color/style: PascalCase expression (Attention=critical / Warning=warning)
  - title: 🚨 status — alertname
  - FactSet: Severity / Team / Service / Summary / Description / Starts At
  - Buttons: 🔍 Alertmanager + 📖 Runbook (coalesce fallback URL)
```

Cross-tenant binding:
- Power Automate ortamı: **Mikrolink (default)** = acik.com tenant
- Microsoft Teams connector: `halil.kocoglu@serban.com.tr` (Serban İnşaat tenant — Power Automate trial activated)
- Bot attribution: "İş akışları" / "Power Automate" — Halil-Serban kişi adı GİZLİ (Flow bot mode)

## 3. 6-Layer Evidence Tablosu (Codex `019e6bd1` AGREE pattern)

| Katman | Kanıt | Komut / Konum |
|---|---|---|
| **1. Source/GitOps desired** | PR #1095 ADR + PR #1096 helm/ESO atomic + PR #1098 runbook V2.1 + PR #1099 D43 SMTP dormant — hepsi MERGED main | `git log --oneline origin/main \| grep -E "1095\|1096\|1098\|1099"` |
| **2. Secret delivery** | ESO `perf-alertmanager-teams-secrets` (monitoring ns, ClusterSecretStore `vault-platform-gitops`, refreshInterval 1h) SecretSynced=True; decoded URL 290 char (Vault'tan stdin-pipe seed via D43 pattern); secret value loglanmadı (length kontrol only) | `kubectl --context k3d-prod -n monitoring get externalsecret perf-alertmanager-teams-secrets -o wide` → `Ready=True`; `kubectl ... get secret perf-alertmanager-teams-secrets -o jsonpath='{.data.TEAMS_WEBHOOK_URL}' \| base64 -d \| wc -c` → 290 |
| **3. Operator reconciliation** | `auth_username_file not found in type config.plain` error son 5 dk içinde YOK (PR #1099 sonrası); Alertmanager CR `spec.secrets[]` apply edilmiş (`[perf-alertmanager-teams-secrets, alertmanager-fallback-secrets]`) | `kubectl --context k3d-prod -n monitoring logs deploy/kube-prometheus-stack-operator --since=5m \| grep auth_username_file` → boş; `kubectl ... get alertmanager kube-prometheus-stack-alertmanager -o jsonpath='{.spec.secrets}'` |
| **4. Runtime mount** | `/etc/alertmanager/secrets/perf-alertmanager-teams-secrets/TEAMS_WEBHOOK_URL` symlink mount, 290 byte; pod age fresh (helm REVISION 4 + manual delete force restart) | `kubectl --context k3d-prod -n monitoring exec alertmanager-kube-prometheus-stack-alertmanager-0 -c alertmanager -- wc -c /etc/alertmanager/secrets/perf-alertmanager-teams-secrets/TEAMS_WEBHOOK_URL` → 290 |
| **5. Runtime config** | Alertmanager `alertmanager.env.yaml` config: `perf-alerts-teams` receiver `webhook_configs.url_file` doğru path; `direct-fallback` receiver name-only/empty (BL-008 dormant); route topology: 2× `team=perf` routes (critical + catch-all) → perf-alerts-teams continue:true; D43 route `NotifyServiceDown/NotifyServiceAbsent → direct-fallback continue:true → severity=critical → alarm-receiver-bridge` preserved | `kubectl ... exec ... -- cat /etc/alertmanager/config_out/alertmanager.env.yaml \| grep -B 1 -A 7 perf-alerts-teams` |
| **6. Functional receipt** | (a) Cluster Alertmanager v2 API synthetic alert injection: `POST /api/v2/alerts` HTTP 200; alert active state `receivers: [perf-alerts-teams, alarm-receiver-bridge]` — dual delivery confirmed. (b) Power Automate run-history: 3 başarılı çalışma (02:26 manuel smoke + 03:07 cluster eval + 03:08 PerfAlertCanaryE2E warning) ortalama 7 sn. (c) Teams Denetim/#perf-alerts kanalında **gerçek cluster-driven critical alert** PerfFederationSmokeStatusAbsentTest Adaptive Card rendered (Attention color = critical severity, FactSet tüm alanlar interpolate, runbook coalesce fallback URL active). | Power Automate UI: "28 günlük çalıştırma geçmişi" → 3 Başarılı; Teams web tab perf-alerts kanal screenshot |

## 4. Live Capture Sequence (2026-05-28)

```
~01:30 UTC  Microsoft Teams Power Automate workflow setup (browser, MCP-driven)
            - Halil-Serban hesabı connector authentication
            - Trigger: "When a HTTP request is received" (Premium 90-gün trial activated)
            - JSON schema: Alertmanager v4
            - Action: Post adaptive card → Denetim/#perf-alerts (özel private channel, Halil only)
            - Adaptive Card body: V2.1 §2.2 PascalCase + coalesce fallback
            - Workflow saved + Aç (enabled) + URL üretildi

~02:06 UTC  Security incident: URL kullanıcı tarafından chat'e + bash history'e leak edildi
            (read -s komutu yan yana yapıştırma → shell parse error & history record)
            → URL rotation: Power Automate workflow "Farklı Kaydet" → yeni v2 + eski sil
            → Yeni URL (sig= HMAC farklı) clipboard'a alındı

~02:10 UTC  Vault PROD seed (D43 stdin-pipe pattern, no-token-log):
            printf '%s' "$TEAMS_PERF_WEBHOOK" | ssh halil@staging-sw '
              VAULT_ROOT_TOKEN=$(jq -r .root_token /home/halil/bootstrap-drill/vault-init-prod.json)
              docker exec -i -e VAULT_TOKEN="$VAULT_ROOT_TOKEN" platform-vault-prod \
                vault kv put kv/platform/perf-alertmanager TEAMS_WEBHOOK_URL=-
              unset VAULT_ROOT_TOKEN
            '
            unset TEAMS_PERF_WEBHOOK
            → Vault kv v3 (v1+v2 = pre-existing Slack pattern history; v3 sadece TEAMS_WEBHOOK_URL)

~02:15 UTC  ESO Teams ExternalSecret cluster apply:
            kubectl --context k3d-prod apply -k kustomize/overlays/prod/eso/alertmanager/ -n monitoring
            → SecretSynced=True; eski perf-alertmanager-secrets (Slack) cluster cleanup (delete)

~02:20 UTC  Helm REVISION 3 upgrade kube-prometheus-stack:
            helm --kube-context k3d-prod -n monitoring upgrade kube-prometheus-stack ...
            → BLOCKED: Operator config sync FAIL "auth_username_file not found" (BL-008)

~02:30 UTC  Cross-AI strategic decision (Codex thread 019e6bd1):
            - 3 alternatif: A (D43 SMTP dormant), B (operator upgrade), C (helm rollback)
            - Codex plan-time AGREE: A (en dar blast radius)
            - PR #1099 açıldı + iter-2 REVISE comment-truth + iter-3 precision + iter-4 AGREE
            - All CI PASS + mergeable clean → squash merge + ai-post-merge-cleanup

~03:00 UTC  Helm REVISION 4 upgrade (PR #1099 merge sonrası):
            → Operator config sync OK (auth_*_file hatası gitti)
            → Alertmanager pod manual delete force restart → 6s yeni pod
            → /etc/alertmanager/secrets/perf-alertmanager-teams-secrets/TEAMS_WEBHOOK_URL mounted

~03:06 UTC  Cluster Alertmanager v2 API synthetic alert injection (in-cluster curl pod):
            POST /api/v2/alerts → HTTP 200
            Alert state active, receivers: [perf-alerts-teams, alarm-receiver-bridge]

~03:08 UTC  Power Automate run-history: 3 Başarılı çalışma
            → Teams Denetim/#perf-alerts kanal: gerçek cluster alert (critical)
              PerfFederationSmokeStatusAbsentTest Adaptive Card rendered
              ✅ Attention color (critical severity PascalCase expression)
              ✅ FactSet tüm alanlar (Severity/Team/Service/Summary/Description/Starts At)
              ✅ Action buttons (🔍 Alertmanager + 📖 Runbook coalesce fallback)
```

## 5. Residual / Out-of-Scope

### 5.1 BL-008 board #854 — D43 SMTP prod activation schema fix

PR #1099 D43 SMTP `email_configs` block explicit dormant; receiver `direct-fallback` name-only intentional empty. Mevcut D43 effective coverage: **bridge GitHub Issue only** (route `continue: true` → `alarm-receiver-bridge` sibling). Mevcut Vault'ta `alertmanager-fallback-secrets` SMTP creds synced ama config tüketmiyor.

**Restore path** (BL-008 follow-up PR, ayrı scope):
- Native `auth_username` / `auth_password` fields (Alertmanager v0.26+ supported)
- Vault preprocess pattern: Vault → güvenli render/preprocess → Helm upgrade chain; secret değerleri stdout/log/PR diff'e düşmemeli
- **YASAK**: env-var `$(SMTP_USER)` interpolation — Alertmanager config env expand etmiyor (Codex `019e6bd1` iter-1 clarification)
- **Alternative spike**: `AlertmanagerConfig` CRD `SecretKeySelector` support v0.90.1; CRD-native Secret reference inline secret render'dan daha temiz olabilir; route merge + selector davranışı test gerek
- **Acceptance**: mock SMTP receipt + `ReconciliationFailed` temiz + `amtool config show direct-fallback email` + D43 synthetic SMTP + bridge GitHub Issue dual receipt

### 5.2 Power Automate Premium 90-gün trial expire ~2026-08-25

Trial bittikten sonra options:
- **Operator karar**: Power Automate Premium per-user/per-flow licence satın al (~$15/ay/kullanıcı)
- **Pivot SMTP email**: ADR-0029 Hibrit D → "Hibrit E" rev (SMTP primary + Teams dormant); BL-008 fix sonrası SMTP receiver active
- **Tek-tenant kapsam**: Halil-Serban Power Automate workflow trial expire öncesi backup export (Codex R29 mitigation chain "exported package backup")

### 5.3 Test cluster (k3d-test) Teams pivot deploy edilmedi

PR #1096'da hem prod hem test ESO eklendi ama:
- Test cluster için ayrı Power Automate workflow gerek (Codex `019e267a` test↔prod webhook differentiation pattern)
- Veya tek workflow + farklı `tenant_channel` matcher (multi-tenant pattern)
- Şu an sadece prod cluster LIVE; test cluster ESO Ready=False kalır (Vault'ta TEAMS_WEBHOOK_URL test path'i seed edilmemiş)

### 5.4 Multi-tenant Slack reactivation chain (ADR-0029 D2 dormant)

`externalsecret-perf-alertmanager.yaml.disabled.template` asset-preserved başka tenants için. Reactivation: `docs/runbooks/RB-perf-alerts-slack-reactivation-chain.md` atomic 6-step.

## 6. R29 Risk Status Flip

| State | Status | Date |
|---|---|---|
| Initial PR #1095 (docs scope) | 🔵 PENDING ACTIVATION (Teams not yet rendered) | 2026-05-27 |
| PR #1096 (helm/ESO atomic) | 🔵 PENDING ACTIVATION (rendered config aktif ama operator activation evidence yok) | 2026-05-27 |
| **PR-3b (this Exit #4) — LIVE** | **🟡 Active (live monitoring required; trial expire ~2026-08-25)** | **2026-05-28** |

R29 Active state mitigation chain (R27 7-step mirror):
1. Service-account/team-owned flow (current: Halil-Serban individual; **TODO**: convert to service-account)
2. Exported package backup (Power Automate workflow export → repo `docs/notify/power-automate-perf-alerts-teams-export.zip`; **TODO** trial expire öncesi)
3. Weekly synthetic Teams smoke — **source desired-state added 2026-05-28 (BL-008-bridge PR #1110; Codex `019e6de3` + `019e6e03` REVISE iter-2 absorb)**; live first-fire pending post-apply (manual `kubectl create job --from=cronjob/r29-teams-smoke` acceptance gate per §5.3). Schedule: Monday 09:00 Istanbul weekly (Power Automate trial expire ~2026-08-25 = 3 months ~12 fires; PA run-history 28-day window risk → weekly > monthly cadence). Live `ACTIVE` after first-fire receipt evidence + truth-sync follow-up PR.
4. Defense-in-depth: Teams + bridge GitHub Issue — Teams ✅ ACTIVE; **bridge GH Issue tertiary leg source-restored 2026-05-28 (PR #1110)**; runtime restoration pending operator PAT + Vault stdin-pipe seed + ESO Ready=True + bridge env GITHUB_TOKEN length verify (RB-alertmanager-bridge-gh-token-seed.md §4.1). SMTP fallback BL-008 RESTORED 2026-05-28 (PR #1104).
5. DLP/license/quota preflight (Halil-Serban Serban tenant; **TODO**: license expire monitoring)
6. Flow run-history failed-run monitoring (Power Automate UI 28-gün geçmişi; **TODO**: alert if fail rate > 5%)
7. URL rotation rehearsal (this session zaten yapıldı — leak sonrası rotation drill complete ✅)

## 7. Codex Cross-AI Review Chain

| Iter | Type | Verdict | Key Refinement |
|---|---|---|---|
| 1 | Plan-time | AGREE | Option A en dar blast radius; D43 SMTP zaten broken; comment-out statu quo'yu görünür kılar |
| 2 | Post-impl | REVISE | Comment-truth: `email_configs` komple comment-out (yarım YASAK); receiver name-only preserve; `continue: true` route preservation defense-in-depth; BL-008 explicit follow-up reference; anti-fake-work language "broken state görünür kıl" |
| 3 | Post-impl iter-2 | REVISE | values-prod.yaml line 99/120/273 comment drift cleanup (Slack→Teams + auth_*_file historical truth-sync) |
| 4 | Post-impl iter-3 | REVISE | Precision: ESO mount-listed vs rendered config consumption ayrım netleştirme (Codex önerisi verbatim absorb) |
| 5 | Post-impl iter-4 | **AGREE** | ready_to_merge=true; merge sonrası 5-step verify chain önerisi |

Codex 5-step post-merge verify chain (this evidence doc kapsamı):
1. ✅ Operator log auth_*_file hatası yok
2. ✅ Alertmanager pod yeni config + recreate
3. ✅ Teams secret mount var (290 char)
4. ✅ `amtool config show` perf-alerts-teams visible + direct-fallback name-only
5. ✅ Synthetic perf alert → Teams Adaptive Card + Power Automate Succeeded

**Codex thread bütünlük**: 5 iter, hepsi `019e6bd1-ab1e-7683-9781-eb6ab5ef3893` thread içinde (ADR-0011 §2.4 audit trail).

## 8. PR'lar (Bu Exit #4 closure batch)

| PR | Açıklama | Merge tarihi |
|---|---|---|
| #1095 | docs/adr/0029-hibrit-d-perf-alerts-teams-primary.md + RB-perf-alerts-slack-reactivation-chain.md NEW + risk-register R29 NEW | 2026-05-27 |
| #1096 | helm-values prod/test + ESO `externalsecret-perf-alertmanager-teams.yaml` create + Slack `.disabled.template` rename (asset-preserved) | 2026-05-27 |
| #1098 | docs/runbooks/V2.1-perf-alert-receiver.md Teams-canonical full sweep | 2026-05-27 |
| **#1099** | **D43 SMTP receiver explicit dormant — V2.1 Teams unblock + BL-008 board #854 (this evidence doc'un asıl unblocker'ı)** | **2026-05-28** |

## 9. Sonuç

Faz 23 V2.1 Ops-A perf-alert receiver Teams primary path **LIVE EXECUTION COMPLETE**. ADR-0029 Hibrit D mühürlü; source/desired state + secret delivery + operator reconciliation + runtime mount + runtime config + functional receipt 6 katman tüm kanıt mevcut. Cluster Alertmanager → Power Automate → Teams Adaptive Card chain canlı çalışıyor; Power Automate run-history 3 başarılı + Teams gerçek cluster-driven critical alert receipt.

Residual scope (4 madde): BL-008 board #854 follow-up + 90-gün trial expire planning + test cluster ayrı workflow + multi-tenant Slack reactivation runbook (zaten ADR-0029 D2 ile pattern hazır).

R29 status: 🔵 Pending → 🟡 Active.

V2.1 Exit #4 ✅
