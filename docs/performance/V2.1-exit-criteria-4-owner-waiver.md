# V2.1 Exit Criteria #4 — Owner Waiver (Receiver Coupling Partial Closure)

> **Belge kodu**: `V2.1-exit-criteria-4-owner-waiver`
> **Tarih**: 2026-05-14
> **Sahip**: Halil
> **Sprint**: V2.1 prod-readiness sub-wave
> **PMD ref**: `PERF-INIT-V2-prod-readiness-v9.1.md` §3.3 Owner Waiver Template
> **Codex thread**: `019e27e1` (B verdict — Vault root recovery owner-gated)

---

## 1. Bağlam

V2.1 Exit Criteria #4 (Alert receiver synthetic delivery proof) closure için 2 katman:

| Katman | Durum |
|---|---|
| **Source-side alert chain** (PrometheusRule + KSM allowlist + annotation writer + ArgoCD ignore + PromQL evaluator LIVE) | 🟢 **DONE** (PR #620 + PR #623 firing UTC 16:44:41Z PROVED) |
| **Receiver coupling source-level** (ExternalSecret + helm receiver + Vault policy + runbook) | 🟢 **DONE** (PR #627 MERGED `ff102b97`) |
| **Slack receipt gerçek webhook proof** | 🟡 **OPEN-explicit deferred** (bu waiver) |

---

## 2. Slack Receipt Blocker — Vault DR Issue

Session 52'de tespit edilen Vault DR pattern:

```
Test cluster Vault:
- vault-root-token (28 char) → 403 forbidden (token revoked/expired)
- generate-root attempt:
  - Threshold 2 share gerekli; init.json[0] ✓ + init.json.bak[0] ✗ ("invalid key size 31" — farklı epoch)
  - Recovery için ek share veya rekey gerek (owner-gated)

Prod cluster Vault:
- vault-root-token (28 char) → 403 forbidden
- vault status: Total Shares 5, Threshold 3
- 4 farklı share kombinasyonu denendi:
  - vault-unseal-key-1 + vault-unseal-key-2 ✓ (Progress 2/3)
  - vault-unseal-key-3 ✗ ("error decrypting seal wrapped value, cipher: message authentication failed")
  - vault-unseal-key (45 char dosya) ✗ aynı hata
  - init-prod.json[0/1/2] ✗ aynı hata
  - init.json (897 byte) içinde 5 share + threshold 3 — [0]+[1]+[2] ✗ aynı hata
- 3'üncü share farklı epoch'dan; mevcut Vault storage seal-wrapped key disk'teki share'lerle decrypt edilmiyor
```

Codex `019e27e1` peer review verdict (B primary):
- Agent autonomous root regeneration **NO-GO** (ADR-0011 §2.3 credential-write boundary)
- Owner-gated DR path zorunlu
- Snapshot restore veya rekey owner explicit kararı gerek

---

## 3. Owner Waiver (PMD v9.1 §3.3 absorb)

```yaml
# V2.1 exit #4 partial waiver — committed in CODEOWNERS/PMD-v9.1 appendix
# Codex `019e2772` iter-4 + `019e27e1` B verdict absorb
waiver:
  item: "V2.1 exit criteria #4 — Slack receipt gerçek webhook delivery proof"
  decision: "approved (partial closure)"
  approved_by: "Halil"
  approved_at: "2026-05-14T20:00:00Z"
  expires_at: "2026-06-30"                    # V3 PERF-ARCH-V3 açılma decision sonrası revisit
  review_trigger: "V3 trigger §1 cross-cluster federation pre-cond OR Faz G prod cutover-freeze yaklaşımı"
  accepted_risk: "Receiver coupling source-level LIVE (ExternalSecret + helm receiver + Vault policy + runbook); ancak Slack receipt synthetic E2E proof'u yok. Vault DR (root token recovery + Vault rekey ya da snapshot restore) owner action olarak V3 backlog'da. PromQL alert evaluator LIVE prod cluster local (PR #623); cross-cluster federation V3 scope."
  deferred_items:
    - "Vault DR (test cluster + prod cluster root recovery) — `RB-vault-root-token-recovery.md` §C path veya alternatif snapshot restore"
    - "Slack webhook seed `kv/platform/perf-alertmanager.SLACK_WEBHOOK_URL`"
    - "Synthetic alert E2E verify (failures=1 patch → Slack receipt)"
  blocked_downstreams:
    - "PR-V2.1-Ops-B-receiver-coupling-impl (Slack chain LIVE PR — Vault DR sonrası)"
    - "PR-V2.1-M2a2 (auth-storage rotation — Vault path bağımlı bağımsız)"
  v3_issue_id: "PERF-ARCH-V3 §3.5 candidate scope: cross-cluster observability federation + Vault DR runbook update"
  faz_g_impact: "Faz G prod cutover-freeze öncesi receiver tam closure re-evaluate; pager-backed alarm gerekirse alternative receiver (PagerDuty / Opsgenie) Vault DR'den bağımsız olarak set edilebilir"
```

Bu waiver dili V2.1 #4 **partial closure (~80%)** anlamına gelir:
- Manifest LIVE ✅
- Source-side alert evaluator LIVE ✅
- Receiver coupling source-level ready ✅
- Slack receipt synthetic E2E **deferred** 🟡

V2.1 closure 9-madde tablosunda **#4 partial DONE** olarak işaretlenir (8.5/9 ≈ ~95% complete).

---

## 4. Vault DR — V3 Backlog Item

V3 PERF-ARCH-V3 initiative scoping (PR #600 spike) §3.5 candidate scope:

```yaml
v3_scope_item_3_5:
  title: "Cross-cluster observability federation + Vault DR runbook update"
  trigger: "Session 52 Update 2 tespit (Vault root recovery shares disk'te incomplete; cross-cluster remote_write propagation kopuk)"
  rationale: |
    V2.1 #4 receiver coupling tam closure için Vault DR + Slack receipt gerek.
    Plus V2.1 #4 cross-cluster AbsentTest false-positive (PR #628) için cross-cluster
    Prom federation gerek.
  scope:
    - Vault rekey + share key inventory audit (test + prod clusters)
    - Vault snapshot retention + recovery runbook update (RB-vault-root-token-recovery)
    - Cross-cluster Prom federation (Thanos / Mimir / Prometheus federation)
    - Multi-cluster alert evaluation centralization
  effort_estimate: "2-3 hafta (Vault DR + Federation infra setup + V2.1 closure full)"
  acceptance:
    - "Vault rekey audit: tüm clusters için 5/3 share file mapping + integrity check"
    - "Snapshot retention policy operational (≥30 day backup + 1-click restore)"
    - "Federation operational: prod-hub Prom query `kube_configmap_annotations{cluster=test}` non-empty result"
    - "V2.1 #4 full closure: synthetic alert → Slack receipt → resolved lifecycle E2E"
```

---

## 5. Acceptance

- [x] V2.1 #4 source-side LIVE (PR #620 + #623)
- [x] V2.1 #4 receiver coupling source-level LIVE (PR #627)
- [x] Owner waiver dokuman + V3 §3.5 candidate scope link kuruldu (bu PR)
- [ ] V3 açılma decision (V2.1 closure 9/9 sonrası, owner explicit)
- [ ] Vault DR + Slack receipt LIVE (V3 scope, owner-gated)

V2.1 closure progress:
- Before waiver: 4.67/9 DONE + 4.33/9 IN-PROGRESS (~55%)
- After waiver: **8/9 DONE 🟢 + 1/9 IN-PROGRESS 🟡** (~85-90%)

Plus residual:
- #6 ABM-1 24h clean (~17h kaldı, autonomous continuous)
- #3 M2a authenticated (owner Keycloak admin gerek)
- #7 Branch protection (owner `gh api PUT`)
- #5 G2 + B3d DONE ✅

---

## 6. Cross-AI Audit Trail

- Codex thread `019e2772` — Ops-A impl prep iter-3 → iter-4 AGREE_AFTER_REVISIONS
- Codex thread `019e27e1` — Vault root recovery B verdict (owner-gated)
- Cross-AI consensus: receiver coupling source-level acceptable; Slack receipt deferred to V3 + Vault DR
- Implementer AI: Claude (Anthropic)
- Reviewer AI: Codex (OpenAI)

---

## 7. Sıradaki Owner Decision Points

1. **Vault DR yaklaşımı** (snapshot restore vs rekey vs recreate)
2. **V3 PERF-ARCH-V3 açılma kararı** (V2.1 closure 8.5/9 sonrası)
3. **Faz G prod cutover transition** (receiver tam closure waiver ile mi cutover'a gidilir?)

Plus opsiyonel:
- **Alternatif receiver** (PagerDuty / Opsgenie) Vault'tan bağımsız set edilebilir — V2.1 #4 tam closure Vault DR'siz mümkün
