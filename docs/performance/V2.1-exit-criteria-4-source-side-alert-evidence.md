# V2.1 Exit Criteria #4 — Source-Side Alert Evidence

> **Status**: ✅ SOURCE-SIDE PROVED (PromQL evaluator chain LIVE) — receiver coupling pending Vault write
> **Date**: 2026-05-14
> **Session**: Session 52 (PR #620 Ops-B core impl + this evidence PR)
> **Pattern**: Option C annotation-via-KSM (PrometheusRule + kube-state-metrics scrape)

---

## 1. Bağlam

PMD v9.1 V2.1 Exit Criteria #4 — Alert receiver V2.1 (Slack synthetic) source-to-receiver chain. Bu doc **source-side** kısmının LIVE kanıtını verir (PromQL alert evaluator state transitions). Receiver coupling (Alertmanager route + Slack webhook) Ops-A receiver impl + owner Vault write sonrası ayrı PR'da kapanacak.

Test pattern: `PerfFederationSmokeFailing` rule'unun `expr: kube_configmap_annotations{...annotation_frontend_federation_smoke_io_failures=~"[1-9][0-9]*"} == 1` koşulunu manuel `failures=1` annotation patch ile sustained 5dk for-clause boyunca tetikleme.

---

## 2. End-to-end chain kanıt zinciri

### 2.1 Pre-patch state (UTC 16:39:00)

```bash
$ ssh halil@staging-sw 'kubectl --context k3d-prod -n platform-prod get cm frontend-federation-smoke-status \
    -o jsonpath="{.metadata.annotations.frontend-federation-smoke\.io/failures}"'
0

$ ssh halil@staging-sw 'kubectl --context k3d-prod -n monitoring exec prometheus-...-0 -c prometheus -- \
    wget -qO- "http://localhost:9090/prometheus/api/v1/rules?type=alert"' \
  | jq '.data.groups[] | select(.name=="frontend-federation-smoke") | .rules[].state'
# PerfFederationSmokeFailing: inactive alerts=0  ← baseline
```

### 2.2 Patch annotation `failures=1` (UTC 16:39:30)

```bash
$ ssh halil@staging-sw 'kubectl --context k3d-prod -n platform-prod patch cm frontend-federation-smoke-status \
    --type=json -p="[{\"op\":\"replace\",\"path\":\"/metadata/annotations/frontend-federation-smoke.io~1failures\",\"value\":\"1\"}]"'
configmap/frontend-federation-smoke-status patched

# Verify:
$ kubectl --context k3d-prod -n platform-prod get cm frontend-federation-smoke-status \
    -o jsonpath="{.metadata.annotations.frontend-federation-smoke\.io/failures}"
1
```

### 2.3 KSM scrape pick-up (UTC ~16:40, ~30s sonra)

KSM `--metric-annotations-allowlist=configmaps=[...]` extraArgs ile annotation label'larını metric'e surface ediyor. Prometheus default scrape interval 30s.

Prometheus query:
```promql
kube_configmap_annotations{
  namespace="platform-prod",
  configmap="frontend-federation-smoke-status",
  annotation_frontend_federation_smoke_io_failures=~"[1-9][0-9]*"
} == 1
```
→ result count: 1 (regex match) ✓

### 2.4 Alert state transition (PromQL evaluator chain)

```
UTC 16:43:34  → state=pending alerts=1   (PromQL koşulu match; for: 5m clause aktif)
UTC 16:44:41  → state=firing alerts=1    (for: 5m clause dolu)
```

**Final firing evidence**:
```json
{
  "name": "PerfFederationSmokeFailing",
  "state": "firing",
  "activeAt": "2026-05-14T16:39:41Z",
  "alerts": 1
}
```

5-dakika for-clause başlangıcı UTC 16:39:41 + 5dk = UTC 16:44:41 → firing transition.

### 2.5 Revert ve baseline restoration (UTC 16:46)

```bash
$ ssh halil@staging-sw 'kubectl --context k3d-prod -n platform-prod patch cm frontend-federation-smoke-status \
    --type=json -p="[{\"op\":\"replace\",\"path\":\"/metadata/annotations/frontend-federation-smoke.io~1failures\",\"value\":\"0\"}]"'
configmap/frontend-federation-smoke-status patched

# Post-revert annotation:
$ kubectl --context k3d-prod -n platform-prod get cm frontend-federation-smoke-status \
    -o jsonpath="{.metadata.annotations.frontend-federation-smoke\.io/failures}"
0
```

Alert state'i KSM yeni scrape sonrası `firing → inactive` döner (regex `[1-9][0-9]*` artık match etmez; `failures=0` baseline).

---

## 3. V2.1 Exit #4 closure status

| Aşama | Durum |
|---|---|
| PrometheusRule manifest LIVE | ✅ PR #620 |
| KSM helm allowlist LIVE | ✅ PR #620 |
| Annotation writer LIVE | ✅ PR #620 (CronJob script extension) |
| ArgoCD ignoreDifferences LIVE | ✅ PR #620 |
| **Source-side alert chain (PromQL evaluator pending→firing)** | ✅ **This evidence PR** |
| Alertmanager receiver route | 🟡 Pending (Ops-A impl PR) |
| Synthetic Slack webhook send | 🟡 Pending (owner Vault `kv/platform/perf-alertmanager` write + Ops-A impl) |
| End-to-end pipeline test (alert → Slack message) | 🟡 Pending (Ops-A merge + Ops-B receiver coupling PR) |

**~67% V2.1 Exit #4 progress** (source-side complete; receiver coupling 2 PR + 1 owner action pending).

---

## 4. Bağlantı: V3 PERF-ARCH-V3 trigger

Bu evidence PMD v9.1 §3.3 V2.1 exit criteria #4 source-side closure'ını sağlıyor. V3 PERF-ARCH-V3 (PR #600 scoping) "3 trigger" §1 alert chain LIVE pre-condition'ını **kısmen** karşılıyor (receiver coupling tamamlanınca tam).

---

## 5. Audit referansları

- PR #596 Ops-B PrometheusRule spike (Codex `019e26c5` 2-tur)
- PR #620 Ops-B core impl atomic 8 dosya (Codex `019e273a` 2-tur AGREE_AFTER_FIXES)
- PR #621 Session 52 handoff doc (audit)
- ABM-1 reproducibility soak runbook (PR #589, `docs/runbooks/V2.1-ABM-1-reproducibility-soak.md`)
