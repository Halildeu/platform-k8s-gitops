# Faz 18.4 COMPLETE — Vault Ops Compose Retirement Evidence

**Date:** 2026-04-24
**Scope:** compose `vault-snapshot` + `vault-audit-init` sidecar retirement → host cron authoritative.
**Codex AGREE:** thread `019dc04d` (ready_for_impl=true, guardrail: flock + unique temp + idempotent ensure).
**User hard rule:** "düzgün çalışan sistemleri bozmdan yapalım" + "bekleme yok hızlı ve güvenli".

---

## 1. Plan özeti

Compose vault-snapshot (24h Raft snapshot loop) + vault-audit-init (file/ audit enable one-shot) sidecar'ları için host-native replacement + retirement.

**Kritik kanıt (Phase 2 manuel smoke):**
- Live staging-sw topology: **iki ayrı vault container** `platform-vault-prod` + `platform-vault-test` (D34 per-realm izolasyon compose tier'da).
- Compose sidecar'lar `platform-vault-snapshot-1` + `platform-vault-audit-init-1` ZOMBIE (`entrypoint: /bin/sh`, `sleep infinity`, 0 iş).
- Host cron `bootstrap/vault-snapshot-cron.sh` Apr 20'den beri tek authoritative — 4 gün evidence (log + dosya).

## 2. PR listesi

| PR | Repo | Konu | Durum |
|---|---|---|---|
| #104 | platform-k8s-gitops | Phase 1 repo-only (scripts + runbook + 4 ssot vault runbook migration) | MERGED |
| #105 | platform-k8s-gitops | HOTFIX multi-vault topology restore (prevent Apr 25 02:00 backup skip) | MERGED |
| #552 | platform-ssot | Phase 3 compose blok retirement + deploy script cleanup | OPEN (CI) |

## 3. Phase bazlı kanıt

### Phase 1 (repo-only, PR #104 + #105 MERGED)

**Script değişiklikleri:**
- `bootstrap/vault-snapshot-cron.sh`: multi-vault loop + Codex guardrails (flock paralel-run + unique temp `/tmp/snap-${env}-$$-${ts}.tmp` + 14-gün retention)
- `bootstrap/vault-audit-init-cron.sh` (yeni): multi-vault per-env idempotent ensure

**Runbook + migration:**
- `docs/RB-vault-ops-host-cron.md` (install + rollback runbook)
- `docs/RB-vault-ops.md` (ssot RB-vault.md migration canonical header)
- `docs/RB-vault-kms-autounseal.md` (D8 Aşama 2 referans)
- `docs/RB-vault-approle-setup-stage.md` (ESO AppRole auth)
- `docs/RB-vault-dev-path-migration.md` (Faz 17 dev parity referans)

**Codex guardrail absorb:**
1. flock paralel-run koruma
2. unique temp per-env (legacy compose sidecar /tmp/snap.tmp race hypothesis)
3. idempotent ensure (Vault audit storage kalıcı, restart detection gereksiz)
4. 14-gün retention canonical (compose 7 gün override yok)
5. Token file chmod 600 kalır; scoped policy 18.4.b hardening
6. backup-freshness-exporter zaten `vault-snapshot-*.snap` tarıyor (prod + test subdir)

### Phase 2 (staging-sw live smoke)

**2026-04-24 19:45-19:50 UTC+3:**

```
ssh halil@staging-sw  # 10.9.10.53
bash /home/halil/platform-k8s-gitops/bootstrap/vault-snapshot-cron.sh
# [vault-snapshot] SNAPSHOT prod → /home/halil/platform/backup/vault/prod/vault-snapshot-20260424-1947.snap (tmp=/tmp/snap-prod-1617602-20260424-1947.tmp)
# [vault-snapshot] OK prod size=80K
# [vault-snapshot] SNAPSHOT test → /home/halil/platform/backup/vault/test/vault-snapshot-20260424-1947.snap
# [vault-snapshot] OK test size=60K
# [vault-snapshot] DONE 20260424-1947

bash /home/halil/platform-k8s-gitops/bootstrap/vault-audit-init-cron.sh
# Success! Enabled the file audit device at: file/
# [vault-audit-init] OK prod: enabled audit file at /vault/logs/audit.log
# Success! Enabled the file audit device at: file/
# [vault-audit-init] OK test: enabled audit file at /vault/logs/audit.log
# (2. çalıştırmada: already enabled — idempotent PASS)
```

**Vault health doğrulama (her iki env):**
```
Initialized    true
Sealed         false
HA Enabled     true
HA Mode        active
Path     Type
file/    file
```

**Zombie sidecar rm:**
```
docker stop platform-vault-snapshot-1 platform-vault-audit-init-1
docker rm platform-vault-snapshot-1 platform-vault-audit-init-1
# Kalan: platform-vault-prod (healthy) + platform-vault-test (healthy)
```

**Crontab doğrulama:**
```
0 2 * * *   /home/halil/platform-k8s-gitops/bootstrap/vault-snapshot-cron.sh >> /home/halil/platform-backup-vault-snapshot.log 2>&1
15 2 * * *  /home/halil/platform-k8s-gitops/bootstrap/vault-audit-init-cron.sh >> /home/halil/platform-backup-vault-audit-init.log 2>&1
```

**4-gün live evidence (Apr 21-24 /home/halil/platform-backup-vault-snapshot.log):**
```
[vault-snapshot] OK prod size=36K  (Apr 21)
[vault-snapshot] OK prod size=40K  (Apr 22)
[vault-snapshot] OK prod size=88K  (Apr 23)
[vault-snapshot] OK prod size=84K  (Apr 24)
```
(8 dosya prod+test, size > 20K her tick)

### Phase 3 (cross-repo ssot cleanup, PR #552 OPEN)

**Dosya değişiklikleri:**
- `deploy/docker-compose.prod.yml`: vault-snapshot + vault-audit-init blokları → RETIRED tombstone comment (service-manager pattern)
- `backend/docker-compose.yml`: stage/dev compose aynı retirement (stub zaten zombie idi)
- `deploy/ubuntu/deploy-backend.sh`: 2 yerde compose up listesinden çıkarıldı
- `deploy/ubuntu/platform-start.sh`: Phase-3 up listesinden çıkarıldı

## 4. Zero regression kanıt

| Katman | Durum | Kanıt |
|---|---|---|
| Vault prod unseal | ✓ unchanged | HA active, sealed=false, health 200 |
| Vault test unseal | ✓ unchanged | HA active, sealed=false, health 200 |
| Audit device | ✓ upgraded | file/ enabled her iki env (daha önce zombie idi) |
| Backup akışı | ✓ authoritative | 4-gün history + manuel tick OK |
| Backup freshness exporter | ✓ prom metric | `backup_last_success_seconds{kind="vault"}` < 25h |
| K8s pod sağlığı (49 prod) | ✓ unchanged | vault ESO reachability korundu |
| Compose kalan stack | ✓ config valid | `docker compose config --quiet` PASS |

## 5. 3-realm izolasyon doğrulama

| Realm | Vault container | Token file | Snapshot path |
|---|---|---|---|
| ubuntu prod | platform-vault-prod | vault-init-prod.json | /home/halil/platform/backup/vault/prod/ |
| ubuntu test | platform-vault-test | vault-init-test.json | /home/halil/platform/backup/vault/test/ |
| dev lokal | (k3d Vault dev, kendi PG) | (Faz 17 bootstrap) | (dev path migration RB) |

**D34 contract UPHELD:** her realm'in kendi vault instance + kendi token + kendi snapshot dir + kendi cron iteration.

## 6. Rollback pozisyonu

Compose sidecar'lar zaten zombie olduğu için compose-level rollback yararı yoktu; retirement = dead code temizliği. Acil rollback senaryosu:
- Host cron problem → backup kesintisi → manuel `vault operator raft snapshot save` fallback
- Script bug → önceki commit'e revert + yeniden smoke test (Phase 2 pattern)

## 7. Next

- **18.5-18.7**: 9 app stateless compose (auth-service, user-service, variant-service, core-data-service, report-service, schema-service, api-gateway, discovery-server, openfga K8s duplicate) — user "no-soak" direktifi ile 24h smoke kaldırıldı, direct stop+rm
- **18.8**: Local k3d-dev clean smoke (Mac user trigger)
- **18.9-18.12**: Observability cleanup + 18.11 frontend truth + 18.12 truth closure
- **19**: Split-repo authority transfer (Codex thread `019dc033` 10-step; discovery-server dahil — user note)

## Referans

- PLAN.md §Faz 18.4 (lines 1240-1245)
- Codex thread `019dc04d` AGREE
- ADR-0002 §0.5 D6 + §3.6 vault-policies env-split
- docs/RB-vault-ops-host-cron.md
- bootstrap/vault-snapshot-cron.sh + vault-audit-init-cron.sh
- bootstrap/backup-freshness-exporter.sh
