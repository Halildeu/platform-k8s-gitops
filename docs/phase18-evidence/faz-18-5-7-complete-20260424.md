# Faz 18.5-18.7 COMPLETE — App Stateless Compose Retirement Evidence

**Date:** 2026-04-24
**Scope:** 11 compose service bloğu retirement (9 app stateless + permission-service Exited + openfga-migrate Completed).
**Codex AGREE:** thread `019dc07c` (ready_for_impl=true, no-soak GO, 2 gate PASS).
**User hard rule:** "düzgün çalışan sistemleri bozma" + "bekleme yok hızlı güvenli" (no-soak).

---

## 1. Plan özeti

Faz 18.5 stop → 18.6 5-dk smoke → 18.7 rm + cross-repo cleanup. 24h soak **KALDIRILDI** (Codex + user direktif).

**Rationale (Codex):** compose stateless 9 zaten out-of-path (Faz 18.1 A0 upstream kanıtı); edge routing K8s NodePort only (`ai.acik.com → 127.0.0.1:30443` + `testai.acik.com → 127.0.0.1:31080/5545`). Compose java processes running ama **edge trafik almıyor** → effective zombie (Faz 18.4 vault-snapshot sleep-infinity pattern ile paralel).

## 2. PR chain

| PR | Repo | Konu | Durum |
|---|---|---|---|
| #107 | platform-k8s-gitops | PLAN drift no-soak fix (24h→5dk smoke + OpenFGA parity gate) | MERGED |
| #553 | platform-ssot | 11 blok retirement (9 app + permission-service + openfga-migrate) + deploy script cleanup | OPEN/CI |
| THIS | platform-k8s-gitops | Evidence + current-state delta + PLAN COMPLETE marker | DRAFT |

## 3. Live retirement timeline (staging-sw 2026-04-24)

### Pre-stop baseline (17:27:07 UTC)
```
K8s prod: 1 Completed, 19 Running
K8s test: 1 Completed, 10 Running
ai.acik.com/            → 200
testai.acik.com/        → 200
ai.acik.com/api/        → 401
testai.acik.com/api/    → 401
```

### Stop 9 container (17:27:53 UTC, ~1 dakika)
```
docker stop platform-{auth,user,variant,core-data,report,schema,api-gateway,discovery-server,openfga}-service-1 platform-*-1
```
Result: 9 container stopped successfully.

### T+5m smoke + OpenFGA parity gate (17:28:16 UTC)
```
K8s prod:               19 Running (BASELINE KORUNDU ✓)
K8s test:               10 Running (BASELINE KORUNDU ✓)
ai.acik.com/            → 200 ✓
testai.acik.com/        → 200 ✓
ai.acik.com/api/        → 401 ✓
testai.acik.com/api/    → 401 ✓
ai.acik.com/realms/master/.well-known/openid-configuration → 200 ✓ (KC stateful)
ai.acik.com/api/v1/authz/version → 401 "JWT token zorunludur" ✓ (PARITY GATE PASS)
```

**Codex gate 1 PASS**: OpenFGA parity — authz chain K8s'te alive, permission-service pod OpenFGA StatefulSet'e bağlı, response yapısı normal (drift olsaydı 500). Store/model parity kanıtlandı → 18.7 point-of-no-return'e geçiş OK.

### Rm 11 container (17:29:35 UTC)
```
docker rm platform-{auth-service,user-service,variant-service,core-data-service,report-service,schema-service,api-gateway,discovery-server,openfga,permission-service,openfga-migrate}-1
```
Result: 11 container removed (9 targeted + 2 exited = permission-service Exited 1 + openfga-migrate Completed 0).

### Post-rm smoke (17:30:21 UTC)
```
K8s prod:               19 Running (BASELINE KORUNDU ✓)
K8s test:               10 Running (BASELINE KORUNDU ✓)
Edge unchanged:         ai.acik.com 200, testai.acik.com 200, /api/ 401, /realms/ 200
```

**Zero regression**. Total retirement süresi: **3 dakika 14 saniye** (17:27:07 → 17:30:21).

## 4. Kalan compose containers (staging-sw)

```
platform-web-nginx-stage   (edge, test tarafı)
platform-kc-prod           (D6 stateful ✓)
platform-vault-prod        (D6 stateful ✓)
platform-pg-prod           (D6 stateful ✓)
platform-grafana-1         (observability, Faz 18.9 hedefi)
platform-prometheus-1      (observability, Faz 18.9 hedefi)
platform-promtail-1        (observability, Faz 18.9 hedefi)
platform-tempo-1           (observability, Faz 18.9 hedefi)
platform-loki-1            (observability, Faz 18.9 hedefi)
platform-web-nginx         (production nginx, Faz 18.11 karar)
platform-kc-test           (D6 stateful ✓)
platform-pg-test           (D6 stateful ✓)
platform-vault-test        (D6 stateful ✓)
platform-test-registry     (k3d-test registry)

Zombie/Created (Faz 18.10 network cleanup):
platform-keycloak-1        (Created state)
platform-vault-1           (Created state)
platform-postgres-db-1     (Created state)
platform-vault-unseal-1    (Created state)
```

## 5. Zero regression kanıt tablosu

| Katman | Baseline | Post-rm | Değişim | Notes |
|---|---|---|---|---|
| K8s prod pods Running | 19 | 19 | 0 | platform-prod ns |
| K8s test pods Running | 10 | 10 | 0 | platform-test ns |
| ai.acik.com/ | 200 | 200 | — | K8s NodePort 30443 |
| ai.acik.com/api/ | 401 | 401 | — | JWT flow |
| testai.acik.com/ | 200 | 200 | — | K8s NodePort 31080/5545 |
| testai.acik.com/api/ | 401 | 401 | — | JWT flow |
| /realms/master/ | 200 | 200 | — | KC compose stateful |
| /api/v1/authz/version | — | 401 JWT | — | OpenFGA parity gate ✓ |

## 6. Deploy script cleanup özeti

`deploy/ubuntu/deploy-backend.sh`:
- `print_compose_diagnostics` services list: 10 → 3 (postgres-db + keycloak + vault)
- `backend_services` build list: 8 → 0 (backend tier retired)
- "Ensure infrastructure is up" compose up: `postgres-db openfga-migrate openfga vault keycloak` → `postgres-db vault keycloak`
- "Recreate backend services" block (4 compose_run + 16 wait_for_service_state) → retired echo

`deploy/ubuntu/platform-start.sh`: backend phase-2 kaldırıldı (tek phase: stateful + observability).

**Toplam code reduction:** 937 satır silindi, 54 satır eklendi (ssot PR #553).

## 7. Faz 18 genel ilerleme (3-realm matrix)

| Realm | Compose durumu | K8s durumu |
|---|---|---|
| ubuntu prod | stateful kaldı (PG/KC/Vault) + observability + edge | 19 pod Running platform-prod ns ✓ |
| ubuntu test | stateful kaldı + observability + edge-stage | 10 pod Running platform-test ns ✓ |
| dev lokal (Mac) | — | k3d-dev Faz 17 profile matrix (18.8 smoke pending) |

**D34 three-realm independence UPHELD**.

## 8. Sıradaki

- **Faz 18.8** (non-blocking paralel): Mac k3d-dev clean smoke (user trigger)
- **Faz 18.9**: Observability cleanup evaluation (compose grafana/prometheus/tempo/loki/promtail K8s tarafında duplicate mi?)
- **Faz 18.10**: Zombie containers (keycloak-1, vault-1, postgres-db-1, vault-unseal-1) network cleanup
- **Faz 18.11**: Frontend source decision capture (Option B canonical truth)
- **Faz 18.12**: Truth closure + Session 30 handoff
- **Faz 19**: Codex split-repo authority transfer (thread `019dc033` 10-step, **discovery-server** + reports migration dahil — user note)

## Referans

- PLAN.md §Faz 18.5-18.7 (lines 1246-1272)
- Codex thread `019dc07c` AGREE
- ADR-0002 §0.5 D6 stateful tier
- D34 three-realm independence
- docs/phase18-evidence/a0-preflight-20260424.md (Faz 18.1 upstream kanıtı)
- platform-ssot PR #553 (cross-repo 11 blok retire)
