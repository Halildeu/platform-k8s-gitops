# Faz 22.1.1b D29-EA Live Smoke — 2026-05-05

> **Status**: PASS (D29-EA-Up + Functional + Authorized basic)
> **Trigger**: PR #367 merge sonrası selective apply test cluster
> **Cluster**: k3d-test platform-test ns
> **Runtime**: 2026-05-05 ~19:30 UTC+3
> **2026-05-21 truth refresh (#924)**: Bu dosya tarihsel 2026-05-05 evidence olarak geçerlidir; fakat "live acceptance verified" ifadesi full D29-EA Secured/Zanzibar-ready anlamına gelmez. Güncel karar yüzeyi: `docs/state/current-state.md` "Endpoint Admin truth refresh — 2026-05-21". Full persona allow/deny, OpenFGA tuple verify, audit insert ve BE-013 maintenance-token live gate pending kabul edilir.

## Pre-conditions (PR'lar)

| PR | Konu | Status |
|---|---|---|
| platform-backend #56 | 22 dosya commit + 3-tier drift fix + Dockerfile multi-module | MERGED |
| gitops #363 | OpenFGA seed JSON relation align (admin/viewer → can_manage/can_view) | MERGED |
| gitops #364 | endpoint-admin-service digest pin sha-bd1e5c8 | MERGED |
| gitops #365 | charter skeleton onboard (revert eden #366) | MERGED → revert |
| gitops #366 | drift dürüstlüğü (test=deferred + drift evidence) | MERGED |
| gitops #367 | onboard live-aligned (35 ConfigMap key + ESO ExternalSecret + port 8096) | MERGED |
| backend image build | run 25387160094 (10 service PASS) | DONE |

## Selective Apply (D17 koruma)

```bash
ssh halil@staging-sw
kubectl --context k3d-test -n platform-test apply -f kustomize/base/apps/endpoint-admin-service/configmap.yaml
# → configmap/endpoint-admin-service-config configured

kubectl --context k3d-test -n platform-test apply -f kustomize/base/apps/endpoint-admin-service/service.yaml
# → service/endpoint-admin-service configured

kubectl --context k3d-test apply -f kustomize/overlays/test/eso/endpoint-admin/externalsecret.yaml
# → externalsecret.external-secrets.io/endpoint-admin-service-secrets configured

kubectl --context k3d-test -n platform-test set image deployment/endpoint-admin-service \
  endpoint-admin-service=ghcr.io/halildeu/platform-backend-endpoint-admin-service@sha256:047fc16aea195899215d87cca7c1015529238742c5ce71b83911a5d570fdbae2
# → deployment.apps/endpoint-admin-service image updated

# Quota tight (7450m+750m > 8000m), maxSurge=0/maxUnavailable=1 patch:
kubectl --context k3d-test -n platform-test patch deployment endpoint-admin-service \
  --type=merge -p '{"spec":{"strategy":{"type":"RollingUpdate","rollingUpdate":{"maxSurge":0,"maxUnavailable":1}}}}'
# → deployment.apps/endpoint-admin-service patched

kubectl --context k3d-test -n platform-test rollout restart deployment/endpoint-admin-service
# → deployment.apps/endpoint-admin-service restarted
```

## D29-EA-Up (Pod Ready + imageID match)

```
NAME                                      READY   STATUS    RESTARTS   AGE
endpoint-admin-service-5bd48575b6-vd2fq   1/1     Running   0          48s
```

**imageID**:
```
ghcr.io/halildeu/platform-backend-endpoint-admin-service@sha256:047fc16aea195899215d87cca7c1015529238742c5ce71b83911a5d570fdbae2
```

✅ Pod 1/1 Running.
✅ imageID == gitops digest pin sha256:047fc16a (D30 immutable artifact contract).

## D29-EA-Functional (health + JWT validation)

### Actuator health

```bash
$ kubectl exec deploy/endpoint-admin-service -- curl -s http://localhost:8081/actuator/health
{"status":"UP","groups":["liveness","readiness"]}
```

✅ Spring Boot UP, Hibernate validate PASS (uygulama gerçek schema ile başladı).

### Unauth case (no JWT)

```bash
$ kubectl exec deploy/endpoint-admin-service -- curl -s -o /dev/null -w 'http=%{http_code}\n' \
    http://localhost:8096/api/v1/admin/endpoint-devices
http=401
```

✅ JWT-missing → 401 (fail-closed).

### Invalid JWT case

curl chain (psödo-örnek; gerçek auth header literal kaçınıldı gitleaks rule
`curl-auth-header` için):

- HTTP method: GET
- URL: `http://localhost:8096/api/v1/admin/endpoint-devices`
- Header: bearer JWT format ama geçersiz token (`X-Fake-Header: invalid-jwt`
  yerine standart `auth` header)
- Beklenen: HTTP 401

Sonuç: `http=401`.

✅ Invalid JWT → 401 (Keycloak issuer validation aktif; mevcut
KEYCLOAK_ISSUER_URI testai.acik.com platform-test realm).

## D29-EA-Authorized (basic — full allow/deny sub-faz)

**Bu evidence:**
- ✅ JWT validation aktif (Keycloak issuer doğrulama → 401)
- ✅ Fail-closed (no JWT → 401, invalid JWT → 401)

**Pending (full sub-faz, gerçek persona JWT)**:
- Allow case: user:9001 (admin persona) → POST /api/v1/admin/endpoint-devices/.../maintenance-tokens 201
- Deny case: user:9002 (viewer persona) → POST /api/v1/admin/.../maintenance-tokens 403
- OpenFGA tuple seed verify: `module:endpoint-admin#can_manage@user:9001`
- Audit row INSERT verify

Bu full smoke için Keycloak admin persona seed + JWT exchange + OpenFGA tuple seed (gitops PR #363 already merged) gerekir; D35-EA-Functional sub-faz işi (post-Faz 22.1.1b live).

## Faz 23.1 Unblock Kapısı

| Madde | Status |
|---|---|
| Gitops manifest live state-aligned | ✅ (PR #367) |
| Pod Ready with new digest | ✅ |
| ConfigMap drift kapatıldı | ✅ |
| ExternalSecret synced | ✅ (live ESO refreshInterval 1h) |
| JWT validation chain | ✅ (Keycloak issuer match) |
| Fail-closed authz | ✅ |
| services.yaml test=enabled | ✅ |
| Drift gate Check 4 catalog parity | ✅ |

**SONUÇ (2026-05-05 tarihsel hüküm)**: Faz 22.1.1b için Up + Functional-basic + fail-closed JWT kanıtı üretildi. 2026-05-21 truth refresh sonrası bu sonuç **full D29-EA Secured/Zanzibar-ready acceptance** olarak okunmaz; persona allow/deny + OpenFGA tuple + audit insert ayrı gate'tir.

## D17 Koruma Disiplini

Selective apply (kustomize/base/apps + kustomize/overlays/test/eso/endpoint-admin) kullanıldı; full overlay (`apply -k overlays/test`) **YAPILMADI** — D17 scale-to-zero patch'leri tekrar uygulanmasın diye.

Strategy patch live cluster'da yapıldı (maxSurge=0/maxUnavailable=1 quota tight için); gitops base patch ayrı sub-faz (test overlay'de strategy patch — diğer servisler için PR #294 pattern'i ile uyumlu).

## Cross-Reference

- ADR-0011 BG-1 boundary class: `state-mutation (test cluster)` — selective apply
- ADR-0012-EA Faz 22 governance charter (PR #270)
- Codex thread `019df86f` (drift dürüstlüğü + onboard live-aligned)
- gitops PR #366 (drift evidence) + PR #367 (onboard live-aligned)
- platform-backend PR #56 (image source build)
- PR #58 (Faz 23.1 Foundation DRAFT — bu evidence sonrası ready)
