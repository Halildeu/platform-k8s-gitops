# PostgreSQL Prod Instance — ADR-0002 Same-Host Full Isolation
# Bind-mount: /srv/platform/stateful/prod/postgres (forward-extension: ayrı partition)
# Network: platform-prod-net (k3d-prod + prod stateful ONLY)
# Port: 5432 (prod)
# Container: platform-pg-prod

services:
  postgres:
    image: postgres:16-alpine
    container_name: platform-pg-prod
    restart: unless-stopped
    ports:
      # Host bind ONLY localhost (127.0.0.1) — k3d-prod pod'lar platform-prod-net
      # üzerinden container DNS (platform-pg-prod:5432) ile ulaşır, host port
      # yalnız admin/debug için gerekli. External exposure YOK.
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD_FILE: /run/secrets/pg_password
      POSTGRES_DB: postgres
      # DB'ler migrate-init.sql ile yaratılır (bkz init/01-create-databases.sql)
    volumes:
      - /srv/platform/stateful/prod/postgres:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d:ro   # DB create + grant scripts
    secrets:
      - pg_password
    # 2026-05-20 vault-prod incident revealed a wider class: all three
    # host-bridge services on platform-prod-net (postgres, keycloak, vault)
    # were drifted between docker-assigned IPs and the k8s overlay Endpoints
    # committed in kustomize/overlays/prod. Argo manual-sync had masked the
    # drift; the next re-sync would have flipped Endpoints back to stale
    # IPs and broken all three cluster→host bridges. Fix: static-pin every
    # Endpoints-bridged host-compose container to its live IP so overlay
    # and runtime stay canonical across restarts. See docs/RB-vault-ops.md
    # §"Network drift" for the reserved-IP allocation table.
    networks:
      platform-prod-net:
        ipv4_address: 172.21.0.10
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    deploy:
      resources:
        limits:
          memory: 3G        # ADR §7.2 platform-prod-stateful.slice
          cpus: "1.5"

networks:
  platform-prod-net:
    external: true         # pre-created by bootstrap: docker network create platform-prod-net

secrets:
  pg_password:
    file: ./secrets/pg_password.txt   # chmod 600, .gitignore
